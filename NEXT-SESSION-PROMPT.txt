Talvora-MCP projesine kaldığımız yerden devam et.

İLK OLARAK şu canonical handoff dosyasını oku:
C:\Users\tayla\Talvora-MCP\HANDOFF-2026-09-19.md

Repo:
C:\Users\tayla\Talvora-MCP

Aktif geliştirme dalı:
refactor/deep-codebase-cleanup

MCP endpoint:
http://127.0.0.1:7676/mcp

Bu handoff, son derin kod temizliği/refactor çalışmasının tamamını içeriyor. Baştan broad audit yapma ve daha önce geçen testleri tekrar koşturma.

TEMEL KURALLAR:
- Windows-native geliştir.
- TAM YETKİ felsefesi; güvenlik/gizlilik bahanesiyle path/command/host/package/user/session yetenek kısıtı ekleme.
- Chocolatey kullan; WinGet kullanma.
- Yazılım geliştirme/library işlerinde Context7 + resmi dokümantasyon kullan.
- Playwright'ı Talvora'ya ekleme; ayrı ChatGPT entegrasyonu olacak.
- Aynı broad testleri tekrar tekrar yapma.
- NEYİ DEĞİŞTİRDİYSEN SADECE ONU TEST ET.
- Statik kontrol yeterliyse gereksiz runtime testi yapma.
- Onay beklemeden ilerle.
- Uzun işlerde kısa ilerleme bilgisi ver.
- GitHub push yalnız ben istediğimde yap.

SON CANONICAL DURUM:
- TalvoraToolManifest = 159 tool.
- Legacy alias'lar kaldırıldı:
  talvora_vs_instances
  talvora_windows_sdk_list
  talvora_vsdev_environment
- Talvora.Shared oluşturuldu.
- ProcessRunner, CommandResolver, TalvoraHttp, AtomicFile, JsonFileStore, FileLog, TextLines ve WindowsSessionLauncher ortak katmana alındı.
- SessionTools artık ince MCP adapter.
- Native installer canonical kurulum motoru.
- scripts\Install.ps1 yalnız bootstrap + native installer runner.
- Installer LocalSystem çalışırsa aktif interactive kullanıcı SID/profile/registry/session bağlamını doğru kullanıyor.
- Installer/Tray/Tool monolitleri domain bazında partial dosyalara bölündü.
- 3500+ satırlık Smoke Program.cs küçük orchestrator + scenario dosyalarına ayrıldı.
- Production kodunda Thread.Sleep, .Result, .Wait(), async void ve boş catch{} kalmadı.
- CI 159-tool manifest ve yeni partial-source mimarisine göre güncellendi.
- PowerShell script çağrılarında $?; native process çağrılarında $LASTEXITCODE kullanılıyor.

ZATEN DOĞRULANDI — GEREKSİZ YERE TEKRARLAMA:
- Talvora + Talvora.Shared Release build: 0 warning / 0 error.
- Smoke project Release build: 0 warning / 0 error.
- Tray build: 0 warning / 0 error.
- Installer direct compile: 0 warning / 0 error.
- Native installer packaging daha önce bu refactor akışında başarıyla Talvora-Setup.exe üretti.
- NativeInstallerSourceRegression: GREEN, 159-tool contract dahil.
- Değişen PowerShell script parser kontrolü: GREEN.
- git diff --check: clean.
- Geçici Payload.zip ve split helper temizlendi.

ÖNEMLİ:
- Dosyaları sırf satır sayısını azaltmak için parçalama. Gerçek sorumluluk sınırı varsa ayır.
- JobTools uzun ömürlü interactive process yönettiği için ProcessRunner'a zorla taşınmayacak.
- Native installer logic tekrar PowerShell'e kopyalanmayacak.
- Windows session P/Invoke kodu tekrar SessionTools'a kopyalanmayacak.
- 159 tool sayısını eski 162'ye çıkarmaya çalışma.

İLK İŞ:
1. Handoff'u oku.
2. git status -sb ve git log -1 --oneline ile dalın temiz/upstream durumunu doğrula.
3. Sonra kullanıcının yeni isteğine doğrudan devam et.
4. Kullanıcı yeni bir spesifik görev vermediyse, yeniden full audit/test yapmak yerine handoff'taki kalan somut adaylardan veya kullanıcı hedefinden ilerle.

Onay istemeden devam et.
