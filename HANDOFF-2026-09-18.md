# Talvora MCP — Session Handoff
Date: 2026-09-18
Repository: C:\Users\tayla\Talvora-MCP
GitHub: bingoweb/Talvora-MCP
Stable MCP endpoint: http://127.0.0.1:7676/mcp
Health endpoint: http://127.0.0.1:7676/healthz

## 1. Current canonical Git state

- Branch: `main`
- Local HEAD: `12d29453b4354cc76f81356a88a476c32f096cb1`
- `origin/main`: `12d29453b4354cc76f81356a88a476c32f096cb1`
- Ahead/behind: `0 / 0`
- GitHub push is complete and verified with `git ls-remote origin refs/heads/main`.
- The latest commit is:
  - `12d29453b4354cc76f81356a88a476c32f096cb1 fix: harden installer payload packaging`

The two transition files are intentionally local working documents unless the user explicitly asks to commit them:
- `HANDOFF-2026-09-18.md`
- `NEXT-SESSION-PROMPT.txt`

## 2. Current live installed runtime

The live installed Talvora service is intentionally one source commit behind repository HEAD because the last commit only changes installer packaging/build logic.

Live service source commit:
`f907ca13cb4f65dfd8324c226986d1f447fb79d4`

Live runtime:
- Product: Talvora
- Version: 3.0.0-dev
- ToolCount: **146**
- Service PID at handoff: 11220
- Service account: `LocalSystem`
- SID: `S-1-5-18`
- Startup: Automatic
- `CanStop=false`
- `CanPauseAndContinue=false`
- Installed version path:
  `C:\Program Files\Talvora\Versions\f907ca13cb4f65dfd8324c226986d1f447fb79d4-20260918183430963\Service\Talvora.exe`

Tray at handoff:
- PID: 8268
- Session: 1
- Path:
  `C:\Program Files\Talvora\Versions\f907ca13cb4f65dfd8324c226986d1f447fb79d4-20260918183430963\Tray\Talvora.Tray.exe`

Business tunnel at handoff:
- tunnel-client PID: 4584
- Session: 1
- Executable:
  `C:\Users\tayla\AppData\Local\Talvora\TunnelClient\bin\tunnel-client.exe`
- Profile: `talvora-business`
- Command uses:
  `--profile-dir C:\Users\tayla\AppData\Roaming\tunnel-client --profile talvora-business`

Runtime state:
`C:\Users\tayla\AppData\Local\Talvora\current.json`

## 3. Installer state

Current installer artifact:
`C:\Users\tayla\Talvora-MCP\artifacts\installer\Talvora-Setup.exe`

Last verified artifact:
- Size: `211695782` bytes
- SHA256:
  `779FA58EBFAB9AB22C91D45F6AD4DC251D73B0C5F233C108505BBCBA133BDBCC`

The installer is transactional and must stay that way:
1. stage a complete versioned payload first,
2. verify staged payload,
3. switch service/tray only after staging,
4. verify service/health/tray after switching,
5. rollback on failure,
6. delete old versions only after success.

Important recent installer hardening:
- payload file manifest validation,
- zip/archive validation,
- retry around transient packaging I/O,
- required payload-file assertions,
- PowerShell 5.1-compatible path handling,
- manifest creation after PDB cleanup,
- build mutex to prevent concurrent installer builds:
  `Global\Talvora.BuildWindowsInstaller.v2`

Do not regress these properties.

## 4. Project rules that remain in force

- Windows-first/native development.
- Talvora is for this user's own machine and follows the project philosophy of **full capability / full access**.
- Do not add arbitrary package/path/tool/command allowlists that reduce intended project capability.
- Existing platform safety rules still apply.
- **Chocolatey is the package manager. Do not introduce WinGet into Talvora workflows.**
- Use **Context7** and official documentation when designing or implementing new developer/runtime/tool integrations.
- Keep Talvora as a LocalSystem Windows service with automatic startup.
- Do not weaken the existing service hardening.
- Preserve the transactional installer/update model.
- Prefer structured MCP tools for common workflows while keeping unrestricted generic escape hatches such as process, PowerShell, Git, Chocolatey, dotnet, npm, Docker, etc.
- Develop substantial changes on a feature branch.
- Merge to local `main` only after the relevant build/source/runtime/smoke gates are GREEN.
- Do not push to GitHub unless the user explicitly asks.
- When the service is replaced during a real installer test, the MCP connection can briefly disappear. Reconnect and continue; do not interpret the first timeout as installer failure.
- The user specifically asked for visible progress. During long work, send a short progress update every 2–3 meaningful tool calls so the UI does not look frozen.

## 5. Completed capability surface — 146 live MCP tools

The exact authoritative list is in:
`C:\Users\tayla\AppData\Local\Talvora\current.json`

Major completed groups:

### Core machine/filesystem/process/service/registry
- system info
- text/binary read/write
- list/copy/move/delete/create directory
- path metadata/hash/find/search/replace
- environment variables
- Windows Event Log
- unrestricted process + PowerShell execution
- process inspection/kill
- service inspect/start/stop/restart
- registry CRUD

### Developer core
- HTTP request/download
- TCP connections/listeners/wait
- project discovery
- command resolution
- ranged/tail/append text
- JSON get/set/delete
- archive create/list/extract

### Long-running jobs
- `talvora_job_start`
- `talvora_job_get`
- `talvora_job_list`
- `talvora_job_read_output`
- `talvora_job_write_stdin`
- `talvora_job_stop`
- `talvora_job_delete`

Jobs persist metadata/logs under ProgramData and support long-running developer servers/watchers/tests.

### Git
- info/status/diff/log/branches
- unrestricted `talvora_git_run`

### Filesystem watch
- start/get/list/read/wait/stop

### Chocolatey
- info/list/search/install/upgrade/uninstall
- unrestricted `talvora_choco_run`

### .NET + Node/npm
- dotnet info/restore/build/test/publish/run
- node info
- npm install/ci/run-script/run

### Interactive Windows sessions
- session list/get
- user-session process start

### Python
- python info/run
- venv create
- pip install/run

### Docker
- info/ps/images/logs/exec/run
- compose run

### Network diagnostics
- network interfaces
- DNS lookup
- ping
- raw TCP exchange
- TLS inspect
- WebSocket exchange

### Configuration formats
- dotenv list/get/set/delete
- INI list/get/set/delete
- XML query/set/delete

### Test reports
- `talvora_test_report_summary`

### Windows native toolchain
- aggregate toolchain discovery
- Visual Studio instances
- VS developer environment capture
- MSBuild info/run with `dotnet msbuild` fallback
- Windows SDK discovery
- CMake info/run
- Ninja info/run
- PE inspection
- Windows file-version metadata

### HTTP mock/webhook server
- `talvora_http_mock_start`
- `talvora_http_mock_get`
- `talvora_http_mock_list`
- `talvora_http_mock_read`
- `talvora_http_mock_reply`
- `talvora_http_mock_stop`

Supports arbitrary HttpListener prefixes, auto replies or manual deferred replies, request capture, query/header/body inspection, caller-controlled responses, and unlimited queue/body limits when set to 0.

## 6. Last verified quality state

Before the final packaging-only commit, the live 146-tool service passed:

- `TALVORA MCP SMOKE GREEN`
- `NATIVE_INSTALLER_SOURCE_GREEN`
- `NATIVE_INSTALLER_RUNTIME_GREEN`

At the packaging commit, the updated installer build script was also exercised under Windows PowerShell 5.1 and produced a successful installer artifact.

Do not assume a future working tree is clean; verify it first.

## 7. Most recent commit sequence on origin/main

- `12d29453` fix: harden installer payload packaging
- `f907ca13` feat: add HTTP mock and webhook tools
- `1cf29470` fix: stabilize Windows toolchain schemas
- `00205e1f` fix: verify and retry installer payload packaging
- `e6586f7e` feat: add Windows native toolchain tools
- `756850d2` feat: add network diagnostics and config format tools
- `8ccc3111` feat: add Python and Docker runtime tools
- `578ebe81` feat: add build runners and interactive session tools
- `0b379836` fix: align watcher manifest and smoke tests
- `e09001a9` feat: add watcher Chocolatey and job cleanup tools
- `e82f5ca5` feat: add background job cleanup
- `736a044f` fix: stabilize background job shutdown
- `f2b08cf1` feat: add config and asset developer tools
- `c3a2f0e8` feat: add development jobs and Git tools
- `d8c82828` feat: add developer core MCP tools

## 8. Required startup procedure for the next session

Do this before new development:

1. Discover/use the existing Talvora local MCP tools.
2. Call `talvora_system_info`.
3. Inspect:
   - Talvora Windows service,
   - `current.json`,
   - repo branch/status/HEAD,
   - `origin/main`.
4. Confirm:
   - live endpoint responds,
   - service is LocalSystem/Automatic,
   - `CanStop=false`,
   - current live ToolCount is 146 unless a later version has already been installed,
   - repository main is at least `12d29453...`,
   - local main and origin/main are synchronized before starting a new feature branch.
5. Do not reinstall the current service merely to make runtime sourceCommit equal the packaging-only repo HEAD. Install only when a new service/runtime change actually needs deployment or when a real installer regression test is intentionally being performed.
6. Read current README/architecture/source regression contracts before adding the next tool group.
7. Use Context7 before implementing external/runtime/toolchain integrations.
8. Continue building high-value application-development capabilities without removing or narrowing existing capability.
9. Keep the user visibly updated every few tool calls.

## 9. Suggested next-development direction

The 146-tool base is already broad. Before adding another large family, inspect the repository docs/source and choose the next developer bottleneck based on missing application-development workflows rather than raw tool count.

Good candidates to evaluate include:
- structured database/client tooling,
- local process/port orchestration improvements,
- build/test artifact analysis,
- package/ecosystem support beyond the already present .NET/npm/Python/Chocolatey paths,
- richer API/integration test orchestration,
- developer logs/traces/profiling,
- Windows-native signing/resource/build workflows.

Do not add a feature only because it increases tool count. Prefer features that reduce friction in real application development while preserving unrestricted lower-level escape hatches.
