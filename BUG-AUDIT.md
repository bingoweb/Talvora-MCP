# Talvora Deep Bug Audit

Last updated: 2026-09-20
Branch: main
Current source HEAD baseline: `747cbfc560c8f7e9d4c3def699ee986d5c164a71` plus intentional dirty development tree; resolve exact final HEAD again before commit.
Current exact-installed audit runtime: `747cbfc560c8f7e9d4c3def699ee986d5c164a71-dirty-8726cde50619`
Canonical exact-installed tool count: 204 unique tools
Status: source remediation is active. Current #121–#171 statuses and counts are below; source-test completion does not imply deployment to the installed service.

## Current remediation status summary — 2026-09-20

- Historical implementation/fix work through #120 includes the completed Control Center, installer/deploy, Playwright CLI migration, Source Edit core, Routing Contract v3, structural adapter and Roslyn semantic adapter work described below.
- The current deep-audit range #121–#171 contains 51 numbered records: **17 OPEN** (**0 CRITICAL, 6 HIGH, 11 MEDIUM**), **31 FIXED** (#121/#122/#123/#125/#126/#127/#131/#134/#135/#137/#138/#141/#142/#144/#145/#146/#147/#149/#150/#152/#154/#157/#158/#160/#161/#162/#163/#164/#168/#170/#171), plus **#143 CLOSED FALSE POSITIVE** and **#166/#167 CLOSED DUPLICATE**.
- Fixed records passed their documented targeted regression or contract gates; remaining OPEN records stay open until their own gates pass. Latest combined Source Edit suite: 60/60 GREEN.
- Remaining work covers #159 response budgets; installer/build provenance and rollback; tunnel/Gitea lifecycle; semantic metadata/concurrency/toolchain fidelity; and #165 durable publication.
- #166 and #167 are not additional defects: their evidence was merged into canonical #147 and #148 respectively.
- Canonical deploy #109 explicit `--silent` behavior must remain intact during all future installer changes.

## Fixed findings

1. Stale hard-coded historical tool counts in tests/CI.
2. Regression tests pinned to historical package versions.
3. ProcessRunner could wait indefinitely after timeout/cancellation if termination failed.
4. Batch-file arguments could be corrupted by cmd expansion/metacharacters.
5. Embedded quote in a batch argument could collapse later argument boundaries.
6. ProcessRunner transport variables leaked into captured Visual Studio developer environments.
7. Failed background-job startup could orphan a child process and job directory.
8. Persisted background jobs could target an unrelated process after PID reuse.
9. Background-job output pump flushed every chunk unnecessarily.
10. Background-job exit observer could hide failures.
11. HTTP mock background failures were insufficiently observable.
12. File watcher disposed-state races and redundant sorting.
13. HttpClient handler/client ownership cleanup.
14. WinForms/Tray lifetime cleanup.
15. Culture-dependent machine-data parsing in multiple tool paths.
16. Visual Studio developer-environment capture quoting.
17. Dirty installer provenance was ambiguous.
18. CI referenced a ProcessRunner regression that did not exist.
19. Broad operational catches in multiple paths were narrowed where appropriate.
20. Installer I/O/metadata formatting cleanup.
21. SQLite backup publication/value handling.
22. Installer/Tray graceful-shutdown event mismatch.
23. Installer could update service while Tray stayed on a stale binary.
24. Installer Tray launch could race the old single-instance mutex.
25. CI WinGet scan recursively inspected generated binary output and could stall.
26. CI WinGet scan also matched the regression assertion that verifies WinGet is forbidden.
27. Dev-server smoke launched the apphost as though it were the dotnet host, so the DLL path was misread as the MCP endpoint.
28. Dev-server readiness failure path could leave a background job artifact behind.
29. Gitea tray health checked only backend port 3001, so a broken Caddy/3000 browser path could still show green; status now checks backend + proxy and exposes a degraded state.
30. Gitea tray restart performed an unnecessary MCP `tools/list` round trip and did not explicitly dispose the manually-created `HttpClientTransport`; it now uses direct `CallToolAsync` and async-disposes the transport per current official C# SDK lifecycle guidance.
31. Gitea tray shutdown could race an in-flight async status/restart operation with disposal of its semaphore; lifetime cancellation now lets operations unwind without disposing coordination primitives underneath them.
32. Gitea Secure MCP Tunnel persistence was incomplete: the logon task returned after connect and the managed runtime later stopped. The watchdog now stays alive for the runtime process, fails on unexpected exit so Task Scheduler retries, has unlimited execution time, and runs PowerShell with `-WindowStyle Hidden` so no terminal window remains open.

## Final runtime verification

- Pre-commit SourceCommit: c8d8e5f602b6cea26c3cb32d698f59b78553721d-dirty-b02df0355735
- Service and Tray run from the same version root.
- current.json ToolCount: 198.
- Current ChatGPT session exposes 198 Talvora tool wrappers.
- Installer log confirmed Tray remained stable after automatic launch.
- Fresh publish from current runtime source matched installed binaries by SHA256:
  - Service/Talvora.dll: match
  - Service/Talvora.Shared.dll: match
  - Tray/Talvora.Tray.exe: match
- Program Files contains only the current Talvora version root after installer cleanup.

## Final verification gates

- Talvora service/shared Release build: GREEN.
- Tray Release build: GREEN.
- Talvora.Smoke Release build: GREEN.
- Canonical native installer build/package: GREEN.
- NATIVE_INSTALLER_SOURCE_GREEN.
- NATIVE_INSTALLER_RUNTIME_GREEN.
- PROCESS_RUNNER_REGRESSION_GREEN.
- FILE_LIST_METADATA_GREEN.
- KNOWLEDGE_SEARCH_SCAN_GREEN.
- RUNTIME_IDENTITY_CACHE_GREEN.
- RUNTIME_METADATA_CACHE_GREEN.
- SMOKE_KNOWLEDGE_ROOTS_ISOLATION_GREEN.
- Business bootstrap self-test on PowerShell 7: GREEN.
- Business bootstrap self-test on Windows PowerShell 5.1: GREEN.
- Final exact deployed 198-tool MCP smoke: GREEN.
- Live batch quote/metacharacter boundary regression: GREEN.
- Live VS developer environment: GREEN with zero internal transport-variable leakage.
- GitHub workflow YAML parse: GREEN.
- git diff --check: GREEN.
- Static TODO/FIXME/HACK/legacy/blocking scan: no material hits.
- NuGet vulnerable-package scan in checked projects: no vulnerable packages reported.
- Gitea tray ModelContextProtocol package: 2.2.0; current package scan clean.
- Context7/official MCP C# SDK lifecycle check: Streamable HTTP client usage, direct tool call and transport disposal aligned with current docs.
- Gitea degraded-path test: Caddy deliberately stopped -> tray status non-green/exit 1 -> tray restart -> backend/proxy healthy again.
- Installed Tray Gitea status/restart integration: GREEN.
- Gitea backend 3001 / proxy 3000 / official MCP 8081: HTTP 200.
- Secure MCP Tunnel: process_running=true, healthy=true, ready=true.
- Hidden watchdog: visible user-session PowerShell window count 0.

## Toolchain verified

- Windows SDK 10.0.28000.0
- Temurin JDK 25.0.4.1 LTS
- Flutter 3.47.5 stable
- Dart 3.13.4 stable
- pnpm 12.4.2
- Yarn Modern 4.18.0
- Bun 1.4.2
- GitHub CLI 2.101.0

## Logging decision

The hourly Talvora-LogMaintenance task bounds tunnel/client and small operational logs and trims completed job stdout/stderr. Live-job stdout/stderr are intentionally left intact until job exit so offsets/full output semantics are preserved.

## Remaining work

Earlier runtime audit is closed. Control Center implementation findings discovered afterward are tracked below.

The historical runtime audit was finalized under an earlier explicit commit/push instruction. The current Control Center batch is intentionally uncommitted because the user has not requested commit/push; verify the authoritative working-tree state with `git status`.

If a future source/runtime change is made, use the mandatory live-update gate:
source -> targeted test -> canonical installer build -> deploy -> system_info/PID/version-root check -> relevant live behavior -> source-vs-installed SHA256 verification when finalizing.

33. [FIXED 2026-09-19 Control Center Faz 1] Ana Talvora tray shutdown yarisi kapatildi. Talvora ve Gitea async operasyonlari lifetime cancellation ile unwind ediyor; koordinasyon semaphore'lari aktif finally bloklarinin altindan dispose edilmiyor. Faz 3 tek-tray refactoru da ayni modeli koruyor.
34. [FIXED 2026-09-20 Canonical Independent Deploy] Canonical installer Talvora LocalSystem MCP servisinin dogrudan child process'i olarak calistirildiginda mevcut Talvora servisini silme/yenileme adimi self-update baglaminda takilabiliyor. 2026-09-19 denemesinde servis Running+Disabled kaldi ve installer `Windows service silinemedi: Talvora` timeout'u verdi. Deploy launcher installer'i Task Scheduler altinda bagimsiz SYSTEM process olarak baslatmali veya installer self-hosted parent/descendant senaryosunu guvenli ele almali. 2026-09-20 resolution: `scripts\Deploy-Windows-Installer.ps1` canonical on-demand hidden SYSTEM Task Scheduler launcher olarak eklendi; installer Talvora service process tree'sinden ayrildi. Windows PowerShell 5.1 parse ve `CanonicalDeployUsesIndependentSystemTask` source regression GREEN.
35. [FIXED 2026-09-19 Control Center Faz 4 hazirligi] Tray startup ve Control Center ayni anda managed-MCP recovery cagirirsa atomic dosya bozulmasa da cift merge/write olusabiliyordu. `ManagedMcpRegistryCoordinator` artik process-wide `RecoveryGate` ile recovery/merge/write zincirini serialize ediyor.

36. [FIXED 2026-09-19 Control Center UI] Canli Control Center penceresi `Wpf.Ui.Controls.FluentWindow` olarak acildiginda gerekli Wpf.Ui control resource/template zinciri uygulama seviyesinde yuklu olmadigi icin kullaniciya beyaz/bos pencere gorunebiliyordu. Urun kararindaki "temel WPF + secici Wpf.Ui" modeline donuldu: ana pencere standart WPF `Window`, Wpf.Ui yalniz secici bagimlilik olarak kaldi. Genisletilmis Control Center smoke dashboard+detail+lifecycle akisini GREEN dogruladi.

102. [FIXED 2026-09-19 Control Center UI] Wpf.Ui FluentWindow acilabiliyor ancak `ThemesDictionary` + `ControlsDictionary` Application resources'a yuklenmedigi icin kullanici oturumunda beyaz/bos pencere gorunuyordu. Wpf.Ui 4.3 Context7 ve paket XML dokumaniyla dogrulandi; `ControlCenterApplication` artik Dark ThemesDictionary ve ControlsDictionary yukluyor. Hedefli visual smoke ile bos/beyaz render regresyonu engellenecek.

37. [FIXED 2026-09-19 Control Center test UX] `--control-center-smoke` gerçek kullanıcı oturumunda görünür WPF pencere açtığı için test sırasında dashboard yaklaşık 2 saniye görünüp kapanıyor ve normal runtime kapanması sanılabiliyordu. Smoke `ControlCenterApplication(smokeTest: true)` ile off-screen, taskbar-disinda çalışacak biçimde ayrıldı; normal Control Center yaşam döngüsü değişmedi.

103. [FIXED 2026-09-19 Control Center Visual Language] Kullanici Fluent tasarimi istemedigini belirtti. `WPF-UI` 4.3.0 bagimliligi tamamen kaldirildi; yerine MIT lisansli `MaterialDesignThemes` 5.3.2 ve Material Design 3 kaynaklari eklendi. Control Center koyu Material 3 control-room tokenlari, outlined arama, ikonlu butonlar ve elevation kartlari kullanacak sekilde guncellendi. Off-screen visual smoke Material kaynaklarini ve render yuzeyini GREEN dogruladi.

38. [FIXED 2026-09-19 Control Center Visual Language] Kullanici Material Design yonunu begenmeyip onceki Wpf.Ui/Fluent tabanina donulmesini istedi. `MaterialDesignThemes` 5.3.2 kaldirildi, `WPF-UI` 4.3.0 geri alindi. Ancak varsayilan demo gorunumu kullanilmadi: Talvora'ya ozgu koyu grafit yuzey, tek soguk mavi vurgu, 8/12/16 spacing sistemi, Segoe UI Variable Text, ince border, dusuk elevation, Wpf.Ui SymbolIcon/TextBox/Button, durum pill'leri ve dengeli kart padding'i uygulandi. Kaynak taramasinda MaterialDesign/PackIcon/ElevationAssist/HintAssist eslesmesi 0; targeted Release build 0 warning/0 error; NuGet vulnerability taramasi temiz; source visual smoke GREEN.

39. [FIXED 2026-09-19 Control Center Wpf.Ui Theme Integration] Wpf.Ui'ye geri donuste iki entegrasyon kusuru hedefli smoke tarafindan yakalandi. (1) `WindowBackdropType.Mica`, `ExtendsContentIntoTitleBar=false` iken InvalidOperationException uretiyordu; Mica zorlamasi kaldirildi, FluentWindow + yuvarlatilmis pencere davranisi korundu. (2) ThemesDictionary/ControlsDictionary yuklu olsa da elle olusturulan Application ilk renderda acik/white theme kalabiliyordu; `ApplicationThemeManager.Apply(ApplicationTheme.Dark, WindowBackdropType.None, updateAccent:false)` eklendi. Son source ve exact-installed visual smoke GREEN.

40. [FIXED 2026-09-19 Control Center Health] Gitea dashboard durumu backend/proxy ile sinirli kaldigi icin Gitea MCP server veya Secure MCP Tunnel bozukken kart yanlislikla Hazir gorunebilirdi. `GiteaTrayClient` artik 3001 backend, 3000 Caddy, 8081 resmi MCP ve tunnel `/healthz` + `/readyz` zincirinin tamamini kontrol ediyor. Exact-installed Gitea restart sonrasinda tum halkalar HTTP 200 / Running olarak dogrulandi.

41. [FIXED 2026-09-19 Control Center Lifecycle] Standard-user Talvora tray, LocalSystem Talvora servisi durduktan sonra MCP broker artik mevcut olmayacagi icin servisi tekrar baslatamiyordu. Installer artik yalniz kurulum kullanicisinin SID'ine Talvora servisi icin query/start/stop/interrogate haklarini veren sinirli service DACL ekliyor; service config/delete/DACL degistirme haklari SYSTEM/Admin'de kaliyor. Canli SDDL ve `MSI\tayla` kullanici tokeniyla stop=0 -> Stopped -> start=0 -> Running -> `/healthz` 200 regresyonu GREEN.

42. [FIXED 2026-09-19 Gitea Lifecycle] Tam zincir Gitea restart ilk uygulamada Caddy `StopPending` durumunda takildi; PowerShell `Stop-Service` kendisi bloklandigi icin PID fallback'ine ulasilamiyordu. Stop istegi `sc.exe stop` ile non-blocking hale getirildi; 4 saniyelik kontrollu graceful pencere sonunda servis durmadiysa ProcessId force terminate ediliyor ve SCM Stopped bekleniyor. Exact-installed `--gitea-restart` GREEN; Gitea/Caddy Running, MCP 8081 HTTP 200, tunnel health/ready HTTP 200.

43. [FIXED 2026-09-19 Talvora Lifecycle] Talvora Windows service host tarihsel olarak `CanStop=false` kullandigi icin Dashboard Durdur eylemi service ACL dogru olsa bile Windows Stop controlunu kabul etmiyordu. `WindowsServiceLifetime.CanStop=true` yapildi; pause kapali, shutdown acik kaldi. Exact deployed service `canStop=true` raporluyor ve standard-user stop/start + health regresyonu GREEN.

44. [FIXED 2026-09-19 Control Center Faz 5] Dashboard/detail lifecycle yuzeyi tamamlandi: kartlarda baglamsal ana eylem, detail'de Start/Stop/Restart, stop confirmation, kullanici-oturumluk manual-stop suppression ve varsayilan kapali Wpf.Ui `CardExpander` teknik ayrintilari eklendi. Talvora tunnel stop/reconnect regresyonu stop=0, reconnect=0, health=true, ready=true; exact-installed Control Center visual smoke GREEN.

45. [FIXED 2026-09-19 Control Center Window Chrome] Control Center `FluentWindow` kullanmasina ragmen icerikte gercek bir `Wpf.Ui.Controls.TitleBar` yoktu; bu nedenle kullanici pencereyi surukleyemiyor ve minimize/maximize/close caption dugmelerini goremiyordu. Explicit Wpf.Ui TitleBar eklendi; close/minimize/maximize acik, ForceShutdown kapali, pencere resize edilebilir ve close yine tray'i kapatmadan pencereyi gizliyor. Visual smoke artik TitleBar renderini, caption control ayarlarini ve kullanilabilir drag surface yuksekligini dogruluyor.

46. [FIXED 2026-09-19 Control Center Window UX] Pencere boyut/konum kaliciligi, ekran disina kacmis kayitlar icin virtual-desktop recovery, dar genislikte responsive header, Ctrl+F / Ctrl+R / F5 / Esc / Alt+Left klavye akislari ve DPI dogrulamasi eklendi. Durumlar renk disinda metinle de aktariliyor; gorunen teknik kopya Turkce ana ifade + gerektiginde parantez ici teknik terim modeline cekildi.

47. [FIXED 2026-09-19 Control Center Faz 6 Recovery] Onceki runtime'da Talvora tunnel icin sinirli reconnect backoff vardi ancak Talvora servisi Offline iken aktif auto-start yapilmiyor, Gitea ise yalniz polling ile izleniyordu. Faz 6 tamamlandi: startup'ta Talvora -> Gitea sirali auto-start/recovery, her MCP icin capped exponential backoff (5/10/20/40/60/120/300s), safe auto-repair, manual-stop exclusion, 4 ardisik hata veya 2 dakika sonrasinda serious-incident classification, ciddi olayda bir kez Control Center auto-open + bildirim ve yalniz ciddi olaydan toparlanmada tek recovery bildirimi eklendi. Exact-installed `--self-test` GREEN. Canli fault injection'da `Gitea MCP Tunnel` task'i elle durduruldu; manuel restart verilmeden 26,5 saniyede `Automatic Gitea MCP recovery succeeded.` logu olustu, Gitea MCP Server + Tunnel Running ve 3001/3000/8081 + tunnel health/ready kontrolleri GREEN oldu.

48. [FIXED 2026-09-19 Tray Reconnect Cleanup] Tray uzerinden Talvora yeniden baglama akisi `ManagedMcpSessionState.ClearManualStop("talvora")` ve recovery-state resetini iki kez arka arkaya cagiriyordu. Davranis bozukluguna yol acmiyordu ancak gereksiz tekrar temizlendi; tek reset akisi korundu.

49. [FIXED 2026-09-19 Control Center Faz 7 Events and Logs] Faz 7 tamamlandi. Current-user `events.json` structured event store; Info=3 gun, Warning=14 gun, Error=30 gun retention; 6 saatlik dedup/grouping; 500 kayit hard cap; dashboard Son olaylar ozeti; ayni pencerede expandable event panel; olay arama/severity filtresi; 2 saniyelik canli raw `tray.log` gorunumu; raw arama/kategori filtresi/kopyalama ve son 512 KB/1200 satir okuma limiti eklendi. `tray.log` 3 MiB + tek `.1` arsiv ile bounded. Recovery/lifecycle/startup olaylari structured store'a baglandi. Source `--self-test` retention/dedup/bounds kontratlarini, visual smoke event/raw-log UI'ini dogruluyor.

50. [FIXED 2026-09-19 Control Center Smoke Placement Isolation] Off-screen visual smoke `ControlCenterWindow(smokeMode:true)` kullanmadigi icin test kapanisinda gercek `window-placement.json` dosyasini -32000 koordinatlariyla ezebilirdi. Application artik smokeMode'u pencereye dogrudan geciriyor; smoke modunda restore/save tamamen kapali. Hedefli testte smoke oncesi/sonrasi placement SHA-256 ayni kaldi.

51. [FIXED 2026-09-19 Control Center Faz 8 Tunnel Provisioning] Secure MCP Tunnel provisioning tamamlandi. Current-user DPAPI Protect/Unprotect round-trip, ayri Admin ve Runtime credential dosyalari, mevcut Runtime key reuse, read-only `admin tunnels get` ile org/workspace scope kesfi, Admin key ile remote tunnel create, 30 saniye activation bekleme, business.json + native profile generation, `runtimes connect`, status process_running/healthy/ready dogrulamasi, generic lifecycle ile tunnel stop/start/restart ve startup provisioning eklendi. Admin key runtime profile/daemon'a aktarilmiyor; Runtime connect argumaninda yalniz `env:CONTROL_PLANE_API_KEY` var. Secret-aware diagnostic redaction self-test GREEN; tray/events log taramasinda key-benzeri eslesme 0. Kurulu v0.0.14 read-only preflight: organization scope=1, workspace scope=1, AdminCredentialPresent=False.

52. [FIXED 2026-09-19 Control Center Faz 9 First Run] Uzun setup wizard yerine dashboard-first first-run/incomplete-setup modeli eklendi. Ilk kullanimda (kayitli pencere konumu yoksa) dashboard otomatik acilir; kritik eksik tunnel kurulumu varsa her acilista tek `Kurulumu Tamamla` karti gorunur. Admin API key yalniz gercekten yeni tunnel olusturulacagi zaman istenir ve current-user DPAPI ile saklanir; mevcut iki tunnel hazir oldugu icin bu makinede Admin key istenmez. Ayrica Settings sayfasi eklenmedi.

53. [FIXED 2026-09-19 Gitea Cross-Process Lifecycle Race] Exact-installed `--gitea-restart` sirasinda ana Tray health dongusu ara Degraded durumu gorup ikinci bir automatic recovery baslatabiliyordu; sonuc saglikli olsa da gereksiz cift lifecycle islemi olusuyordu. Gitea Start/Stop/Restart icin session-local named Semaphore tabanli cross-process operation guard eklendi. Auto-recovery guard doluyken failure/backoff yazmak yerine kullanici isleminin bitmesini bekleyecek sekilde davranir. Guard acquire/block/release kontrati `--self-test` icinde GREEN.

54. [FIXED 2026-09-19 Native Installer Runtime Regression] `NativeInstallerRuntimeRegression.ps1` tarihsel `CanStop=false` beklentisini koruyordu ve Control Center lifecycle ile celisiyordu. Regression artik canli Talvora servisinde `ServiceCanStop=True` ve `ServiceCanPauseAndContinue=False` bekliyor. Exact-installed runtime regression `NATIVE_INSTALLER_RUNTIME_GREEN`; ToolCount=198.

55. [VERIFIED 2026-09-19 Control Center Final Gate] Exact-installed `74601595e4727d1cfd09388ae3c8b4d25dc136fa-dirty-89135c8503e5` icin self-test, Gitea restart, Control Center visual smoke, tunnel provisioning preflight, Talvora reconnect, Gitea status ve native installer runtime regression GREEN. Interactive mutex regresyonunda ikinci `--replace` instance 10 saniye sonra dogru sekilde cikti, canli Tray PID 2356 olarak degismedi. Son ve tek final 198-tool MCP smoke `TALVORA MCP SMOKE GREEN` ile exit 0 tamamlandi.

56. [FIXED 2026-09-19 GitHub Push Authentication Context] `talvora_git_run` LocalSystem hesabinda calistigi icin HTTPS GitHub push'lari kullaniciya ait GCM/OAuth credential kasasini goremedi; credential eksikken GCM gorunmeyen browser/GUI prompt'u bekliyor ve MCP tool call timeout'a dusuyordu. Kalici cozum: GitHub HTTPS push'lari otomatik olarak logged-on Windows kullanicisinin primary token/session'i icinde calistiriliyor; stdout/stderr/exit code LocalSystem MCP'ye geri tasiniyor. Interactive runner push sirasinda `GIT_TERMINAL_PROMPT=0`, `GCM_INTERACTIVE=0`, `GCM_GUI_PROMPT=0` enjekte ediyor; credential yoksa hang yerine hizli fail oluyor. `MSI\tayla` profili GCM OAuth + Windows Credential Manager (`wincredman`) ile `bingoweb` hesabina baglandi. Kullanici-baglamli `git push --dry-run github main` exit 0 ile dogrulandi.

## 2026-09-19 — Playwright MCP sürüm pinleme düzeltmesi
- Risk: İlk runtime hazırlığında `@playwright/mcp` ve `@modelcontextprotocol/sdk` exact sürümle yazılmıştı; bu yeni proje kuralına aykırıydı.
- Düzeltme: dependency specifier'lar `latest` yapıldı ve npm lockfile silindi. Bundan sonra sürüm pinleme yapılmayacak; current stable/latest kullanılacak.
- Durum: KAPALI.


### PW-MCP-001 — localhost Host allow-list port uyumsuzluğu
- Bulgusu: İlk Streamable HTTP initialize denemesi 403 `Access is only allowed at 127.0.0.1, localhost` döndürdü.
- Etki: MCP initialize/tool-list zinciri başlayamıyor; process/listener tek başına Ready sayılmamalı.
- Neden adayı: HTTP Host header port içeriyor (`127.0.0.1:8931`), allow-list port içermiyordu.
- Plan: Host kontrolünü kapatmadan 127.0.0.1:8931 ve localhost:8931 biçiminde düzelt, yalnız initialize/list testini tekrar et.
- Durum: AÇIK.


### PW-MCP-001 kapanış
- Düzeltme: `--allowed-hosts` değeri `127.0.0.1:8931,localhost:8931` olarak portlu Host header ile uyumlu hale getirildi; kontrol devre dışı bırakılmadı.
- Hedefli doğrulama: initialize GREEN, 45 tools/list GREEN, `browser_navigate` GREEN, `browser_snapshot` GREEN.
- Durum: KAPALI.


57. [FIXED 2026-09-20 Source Reconciliation] Port 8931 zaten Talvora LocalSystem -> WindowsSessionLauncher ile Session 1'de baslatilan extension-mode Playwright MCP tarafindan tutulurken gecici smoke scheduled task ayni portta ikinci instance baslatmayi denedi ve EADDRINUSE ile cikti. Kalici cozum: tek canonical launcher/runtime sahibi belirlenecek, gecici task kaldirilacak ve Control Center lifecycle ayni instance'i yonetecek.
58. [FIXED 2026-09-20 Source Reconciliation] Current-user Playwright runtime package.json `latest` kullaniyor ancak npm kurulumunun `node_modules/.package-lock.json` hidden lock metadata'si mevcut. Yeni proje kuralina uymak icin canonical provisioning `--package-lock=false` kullanacak ve hidden lock metadata temizlenecek; runtime package manifest exact version tasimayacak.

## 2026-09-19 — Playwright Extension welcome-tab interference
- Bulgu: `--extension --profile-dir-name Default` ile bağımsız smoke sırasında resmî Playwright Extension kullanıcı Chrome'unda welcome/connection sayfasını tekrar tekrar öne getirerek aktif ChatGPT sekmesini bozdu.
- Etki: Kullanıcı masaüstü akışı kesildi; bu çalışma biçimi production default için uygun değil.
- Düzeltme kararı: Extension modu ana managed runtime olmaktan çıkarıldı. Extension server + smoke/helper node süreçleri durduruldu. Production/smoke modeli ayrı current-user persistent Chrome profile + localhost HTTP + headless browser olarak değiştirildi. Extension yalnız ileride açıkça mevcut oturuma/SSO'ya ihtiyaç olan isteğe bağlı bağlanma yolu olarak değerlendirilebilir.
- Durum: KAPALI.


### PW-MCP-002 — Extension managed-server restart döngüsü kullanıcı Chrome sekmesini bozuyor
- Bulgusu: `--extension` ile kalıcı Scheduled Task çalıştırıldığında extension bağlantı sayfası kullanıcı Chrome'unda açılıyor; bu bağlantı sayfası kapandığında MCP process `Exit=-1` ile sonlanıyor. Task restart policy tekrar bağlantı/welcome sayfası açarak kullanıcının aktif ChatGPT sekmesini kesintiye uğratıyor.
- Etki: Üretim kalitesinde background managed MCP için kabul edilemez kullanıcı müdahalesi ve görünür browser yan etkisi.
- Anlık önlem: `Talvora Playwright MCP` Scheduled Task durduruldu ve Disabled yapıldı; welcome tekrar döngüsü kesildi.
- Kalıcı karar: Managed/autostart Playwright MCP extension modunda çalışmayacak. `MSI\\tayla` oturumunda ayrı Talvora persistent Chrome user-data-dir ile `--browser chrome --headless` kullanılacak. Mevcut Playwright Extension kurulu kalacak ancak managed health/recovery'nin zorunlu parçası olmayacak.
- Durum: DÜZELTİLİYOR.


59. [FIXED 2026-09-19 Playwright Generic Recovery Compile Break] Kesintiden kalan kaynakta `_genericMcpTimer` `MaintainGenericManagedMcpsAsync()` metodunu çağırıyor ancak metod gövdesi yoktu; targeted Talvora.Tray Release build CS0103 ile kırıldı. Kalıcı çözüm: generic managed-MCP health/recovery döngüsü capped backoff + manual-stop suppression + serious incident/event akışıyla tamamlanacak ve timer startup/dispose yaşam döngüsüne bağlanacak.


### PW-MCP-003 — Scheduled Task stop child process ağacını deterministik kapatmıyor
- Bulgusu: `Stop-ScheduledTask` sonrası launcher `pwsh.exe` ve child `node.exe` yaşamaya devam etti; yeni task 8931 portu dolu olduğu için başlayamadı (`LastTaskResult=1`).
- Etki: Start/Restart işlemlerinde eski MCP instance'ı kalabilir ve yeni instance port çakışması yaşayabilir.
- Anlık düzeltme: Eski launcher process tree PID bazlı kapatılacak; yeni supervisor parent-watch ile child cleanup yapacak.
- Kalıcı düzeltme: Generic lifecycle Stop/Restart scheduled-task zinciri Playwright launcher için task stop sonrası ilgili process/port kapanışını doğrulayacak.
- Durum: DÜZELTİLİYOR.


60. [FIXED 2026-09-19 Talvora Service Registration Loss During Upgrade] Gerçek installer logu 22:56:17'de `Service did not accept STOP; deleting registration before terminating PID=2188.` kaydını, ardından 22:56:32'de `TimeoutException: Windows service silinemedi: Talvora` hatasını gösterdi. Eski akış Talvora servis kaydını upgrade sırasında `sc delete` ile siliyor, fakat rollback yalnız `CreateServiceAsync` başarılı olduktan sonra set edilen `switchedService` koşulunda çalışıyordu. Böylece deletion sonrası timeout/CreateService öncesi hata Windows'ta Talvora servis kaydını tamamen yok bırakabiliyordu. Düzeltme: Talvora upgrade yolu artık servis kaydını hiç silmiyor; recovery actions geçici temizleniyor, normal stop deneniyor, gerekirse process tree force-stop ediliyor, mevcut servis `sc config` ile yeni binPath'e atomik reconfigure ediliyor; servis yoksa yalnız fresh/recovery durumda `sc create` kullanılıyor. Rollback artık service-switch başlar başlamaz arm ediliyor ve kullanıcı cancellation token'ından bağımsız `CancellationToken.None` ile eski binary/service/tray durumunu geri kuruyor. `StopAndDeleteServiceAsync` yalnız gerçek kaldırma/legacy servis temizliği için tutuldu.


61. [FIXED 2026-09-19 Playwright Scheduled Task XML Encoding] İlk güvenli live-deploy regression'ında Talvora servis kaydı korunarak yeni build başarıyla ayağa kalktı; installer 82% Playwright aşamasında `schtasks /Create ... /XML ...` çağrısında `(1,40): encoding değiştirilemiyor` hatası verdi ve yeni rollback yolu eski Talvora binary'sini başarıyla geri yükledi. Kök neden: Task Scheduler XML deklarasyonu/çıktısı UTF-8 idi; Windows native Task Scheduler XML biçimi UTF-16'dır. Düzeltme: XML declaration `UTF-16`, dosya yazımı `Encoding.Unicode` (BOM'lu UTF-16LE) yapıldı. Servis-kaybı yaşanmadı; rollback doğrulandı.


62. [FIXED 2026-09-19 Control Center Smoke Refresh Race] Exact-installed final UI smoke `Control Center dashboard did not load any managed MCP` ile false-negative verdi; aynı ana Tray hemen önce registry Count=3 yüklemişti. Kök neden: pencere açılırken `BringToForeground()` fire-and-forget `RefreshDashboardAsync()` başlatıyor; smoke 2 sn sonra yeniden çağırdığında `_refreshGate.WaitAsync(0)` doluysa ikinci çağrı anında dönüyor ve `_snapshot` henüz null/boş kalabiliyordu. Düzeltme: smoke scenario 15 sn bounded deadline içinde mevcut refresh'in tamamlanmasını/registry snapshot'ın dolmasını bekleyip 250 ms aralıkla yeniden dener. Production dashboard refresh davranışı değiştirilmedi.


63. [FIXED 2026-09-20 Source Reconciliation] `scripts/Build-Windows-Installer.ps1` içinde `$PlaywrightPayload` ataması, payload klasörü oluşturma ve Playwright asset copy/manifest/assert akışı yinelenmiş. Bugün idempotent görünüyor fakat ileride asset listesi/validation ayrışırsa canonical installer payload'ının tutarsız üretilmesine ve gereksiz IO'ya yol açabilir. Tek canonical Playwright payload hazırlama bloğuna indirilmeli.


64. [FIXED 2026-09-20 Source Reconciliation] Kullanıcı kuralına uygun olarak Talvora/Tray NuGet bağımlılıkları `Version="*"` ile her build'de en güncel stable sürümü çözümlüyor. Bu pinleme değildir ve korunmalıdır; ancak installer/runtime metadata yalnız source commit/fingerprint kaydediyor. Aynı commit farklı tarihlerde farklı resolved NuGet sürümleriyle farklı binary üretebilir ve olay incelemesinde hangi dependency setinin canlı olduğu kanıtlanamaz. Çözüm pinlemek değil; canonical build sırasında resolved package name/version listesini artifact/runtime metadata'ya yazmak ve dashboard/version detayında gerektiğinde göstermek.


65. [FIXED 2026-09-20 Source Reconciliation] `ControlCenterComponentHealthService.ProbeProcessAsync` PowerShell 7 yoksa Windows PowerShell 5.1'e düşüyor; üretilen script `string.Contains($marker,[StringComparison]::OrdinalIgnoreCase)` overload'unu çağırıyor. Bu overload .NET Framework/Windows PowerShell 5.1'de yoktur ve fallback health probe MethodException ile bozulur. Mevcut makinede pwsh 7 bulunduğu için canlı sistem etkilenmiyor; ancak fallback sözleşmesi gerçekte çalışmıyor. Çözüm: PS5.1 uyumlu `IndexOf($marker,[StringComparison]::OrdinalIgnoreCase) -ge 0` kullanmak.


66. [FIXED 2026-09-20 Source Reconciliation] `PlaywrightManagedMcpRecoveryDiscovery` yalnız launcher dosyası, package.json veya profile klasöründen en az biri varsa kayıt döndürüyor. Scheduled task/tunnel config/state mevcut fakat bu üç artifact kayıpsa managed registry Playwright'ı tamamen düşürebilir; bu da istenen first-run/incomplete-setup kartını görünmez yapar. Discovery koşulu canonical task ve `McpTunnel\business.json` gibi sahiplik kanıtlarını da hesaba katmalı; eksik artifact'ler card state'i Attention/Incomplete yapmalı, kaydı yok etmemeli.


67. [FIXED 2026-09-20 Source Reconciliation] Generic `ControlCenterLifecycleService` bütün Playwright Start/Stop/Restart scriptlerini `RunPrivilegedPowerShellAsync` ile `http://127.0.0.1:7676/mcp` Talvora LocalSystem broker üzerinden yürütüyor. Oysa `Talvora Playwright MCP` task'ı current-user/Interactive/Highest ve kullanıcı bağlamından doğrudan yönetilebilir. Talvora servisi offline olduğunda Playwright kartı bağımsız olarak Start/Stop/Restart edilemiyor; gerçek tray logu 22:57:46'da `MCP=playwright; Operation=Restart ... connection refused (127.0.0.1:7676)` ile bunu kanıtlıyor. Çözüm generic lifecycle executor'a component ownership/required-privilege bazlı current-user task yolu eklemek; yalnız gerçekten privileged Windows-service işlemlerini Talvora broker'a göndermek. Böylece Playwright recovery Talvora servisinden bağımsız kalır.


68. [FIXED 2026-09-20 Source Reconciliation] `ManagedMcpSessionState` manual-stop bilgisini yalnız static in-memory `ConcurrentDictionary` içinde tutuyor. Tray process replace/crash/restart olduğunda aynı Windows logon session devam etse bile suppression sıfırlanır; `InitializeManagedRegistryAndRecoveryAsync` AutoStart kayıtlarını yeniden başlatabilir. Bu, istenen `manual Stop session suppression` sözleşmesini bozar ve kullanıcı elle durdurduğu Playwright/Talvora/Gitea'nın Tray yenilenince geri açılmasına yol açabilir. Çözüm: current interactive SessionId/logon identity ile scope edilmiş ephemeral session-state dosyası veya named kernel object kullanmak; logoff/yeni session'da otomatik sıfırlamak.


69. [FIXED 2026-09-20 Source Reconciliation] `Start-PlaywrightMcp.ps1` `server.log` için 5 MiB rotate kontrolünü yalnız task başlangıcında yapıyor, ardından supervisor stdout/stderr `*>> server.log` ile task ömrü boyunca aynı dosyaya ekleniyor. Uzun süre çalışan Playwright MCP'de log restart olmadan sınırsız büyüyebilir. `--output-max-size` yalnız Playwright output artifacts içindir, launcher/server logunu sınırlamaz. Çözüm: supervisor/launcher logging'i size-aware rolling writer'a taşımak veya bounded log sink kullanmak; raw-log viewer aynı rolling dosyaları okumalı.


70. [FIXED 2026-09-20 Source Reconciliation] `ManagedMcpOperationCoordinator` yalnız `GiteaTrayClient` tarafından kullanılıyor; `ControlCenterLifecycleService.ExecuteGenericAsync` Playwright için hiçbir named semaphore/operation lease almıyor. Generic recovery timer kendi `_genericMcpOperationGate`'iyle yalnız recovery döngüsünü serialize ediyor, Control Center kullanıcı Start/Stop/Restart işlemlerini serialize etmiyor. Sonuç: auto-recovery ve kullanıcı Restart/Stop aynı anda scheduled task, process tree, tunnel ve smoke state üzerinde yarışabilir. `MaintainGenericManagedMcpsAsync` içindeki `catch (ManagedMcpOperationInProgressException)` Playwright tarafında bugün fiilen erişilemez. Çözüm: `ExecuteGenericAsync` girişinde registration.Id bazlı `ManagedMcpOperationCoordinator.TryAcquire` zorunlu olmalı; UI/recovery aynı lease'i paylaşmalı.


71. [FIXED 2026-09-20 Source Reconciliation] Playwright `--shared-browser-context` ile tüm HTTP clients aynı browser context'i paylaşıyor (Microsoft resmi dokümanı). Health smoke `browser_tabs action=new` ile sekme açıyor fakat cleanup `browser_tabs action=close` çağrısında index vermiyor; resmi tool semantiğinde bu `current tab`ı kapatır. Tunnel bağlandıktan sonra ChatGPT eşzamanlı tab seçerse smoke yanlış sekmeyi kapatabilir ve aktif otomasyonu bozabilir. Çözüm: `new` sonucundan oluşturulan tab index/identity'yi güvenilir biçimde yakalayıp explicit index ile kapatmak ve tercihen gerçek browser smoke'u tunnel connect'ten önce, external clients erişmeden çalıştırmak.


72. [FIXED 2026-09-20 Source Reconciliation] `RecordBrowserSmokeAsync` generation değerini smoke tamamlandıktan sonra `server-start.json`dan yeniden okuyor; smoke başında hangi generation üzerinde çalıştığı capture edilmiyor. Eşzamanlı restart olursa teorik olarak eski client/smoke yeni generation marker'ına yazılabilir. Bug 70'teki generic operation-lock eksikliği bu yarışı mümkün kılıyor. Çözüm: generation'ı smoke başlamadan capture etmek, tool calls sonunda generation'ın değişmediğini doğrulamak ve marker'a yalnız aynı captured generation için atomik yazmak.


73. [FIXED 2026-09-20 Source Reconciliation] `TunnelProvisioningScope` `ReferenceTunnelId` ve `UpdatedAtUtc` saklıyor fakat `GetOrDiscoverScopeAsync` cache'i kullanırken bu alanları hiç kontrol etmiyor. Reusable runtime source farklı bir tunnel'a dönerse veya organization/workspace scope zamanla değişirse eski `tunnel-scope.json` yeni tunnel creation için körlemesine kullanılabilir; yanlış workspace/org scope ile remote tunnel oluşturma ya da creation failure riski var. Çözüm: cache yalnız `ReferenceTunnelId == reference.Config.TunnelId` ve bounded TTL içinde ise kullanılmalı; aksi durumda `tunnels get` ile yeniden keşfedilmeli.


74. [FIXED 2026-09-20 Source Reconciliation] `Assess` ve `FindReusableRuntimeSource` runtime credential hazır kararını `runtime-key.dpapi` dosyasının varlığına göre veriyor; current-user DPAPI decrypt/read doğrulanmıyor. Bozuk/kopyalanmış/başka kullanıcıya ait blob setup'ı tamamlanmış gösterebilir fakat Connect/Provision sırasında patlar. Çözüm: secret içeriğini loglamadan bounded DPAPI decrypt validation eklemek; `HasRuntimeCredential` semantiğini gerçekten kullanılabilir credential olarak tanımlamak.


75. [FIXED 2026-09-20 Source Reconciliation] `Configure-ChatGPT-Business.ps1` artık `latest` release'i çözüyor ve 2026-09-19 itibarıyla resmi latest v0.0.14 ile canlı sistem eşleşiyor. Ancak Control Center startup/provisioning mevcut `BusinessConfig.TunnelClientVersion` ve binary path'ini yeniden kullanıyor; yeni tunnel-client release çıktığında çalışan kurulum otomatik latest-check/upgrade yapmıyor. Kullanıcının sürekli-latest kuralı için periodic/bounded release check + atomic client update mekanizması eksik; pinleme yapılmamalı.


76. [FIXED 2026-09-20 Source Reconciliation] Ayrıntı ekranında alan etiketi `Profil / durum yolu` fakat değer yalnız `registration.ProfilePath` (`...\PlaywrightMCP\profile`). `RuntimeGenerationStatePath`, `BrowserSmokeStatePath` veya Playwright state root gösterilmiyor. Kullanıcının istediği profile/state path görünürlüğü tam değil ve etiket mevcut içeriği olduğundan fazla gösteriyor. Çözüm: profile path ve runtime/state paths ayrı teknik alanlar olarak gösterilmeli.


77. [FIXED 2026-09-20 Source Reconciliation] `BuildDetailView` durum/sürüm, bileşenler, lifecycle ve teknik ayrıntıları gösteriyor ancak selected MCP'ye ait `son olaylar` bölümü yok. Olay listesi dashboard seviyesindeki ayrı expander'da; kullanıcı Playwright ayrıntı ekranında son olayları istemişti. Çözüm: detail view'e selected registration.Id ile filtrelenmiş son N structured event + severity/time özeti eklemek; global dashboard event paneli korunmalı.


78. [FIXED 2026-09-20 Source Reconciliation] `tests/Talvora.Smoke --playwright-mcp` default endpoint'i `http://127.0.0.1:8931/mcp`; production managed registry endpoint'i `8932/mcp` compatibility proxy'dir. Bu test default haliyle proxy'nin OAuth discovery 404 davranışını, header forwarding'ini ve streamable HTTP passthrough'unu regress etmez. Exact-installed managed probe 8932'yi test ediyor ama unit/smoke harness'ta proxy-specific contract testi eksik. Çözüm: production endpoint default 8932 veya ayrı `--playwright-proxy` contract scenario eklemek; backend smoke ayrıca korunabilir.


79. [FIXED 2026-09-20 Source Reconciliation] Canlı kanıt: `browser-smoke.json` passedAt `2026-09-19T23:14:53.8507066+03:00`, fakat mevcut Chrome ana process PID 3704 `2026-09-19T20:24:35.4649172Z` (=23:24:35+03) tarihinde, yani smoke'tan yaklaşık 10 dakika sonra açıldı. MCP backend generation değişmediği için `HasCurrentBrowserSmoke` eski marker'ı hâlâ geçerli sayıyor. Browser demand-launch/crash/relaunch aynı MCP generation içinde gerçekleştiğinde Ready durumu mevcut browser instance için navigate/snapshot kanıtına sahip olmayabilir. Çözüm: browser instance identity/epoch takibi veya bounded smoke freshness + browser relaunch detection; yeni browser instance görüldüğünde marker invalidate edilip güvenli smoke yeniden yapılmalı.


80. [FIXED 2026-09-20 Source Reconciliation] `supervisor.mjs` backend Node child'ı hemen `spawn` ediyor; ardından `writeProcessState()` ve `server.listen()` geliyor. `fs.writeFileSync/renameSync`, child `error` veya proxy listen `error` gibi supervisor-fatal durumlarda central failure handler yok. Windows'ta parent Node exit olması child process tree'yi garantiyle sonlandırmaz; backend 8931'i ve Chrome persistent profile'ı orphan bırakabilir. Scheduled task restart'ı sonra port/profile conflict ile döngüye girebilir. Çözüm: child `error`, server `error`, uncaughtException/unhandledRejection için tek idempotent shutdown; Windows process tree/job-object veya `taskkill /T /F` benzeri deterministic descendant termination; state cleanup yalnız gerçekten tree kapandıktan sonra.


81. [FIXED 2026-09-20 Source Reconciliation] `supervisor.mjs` request handler incoming `req.headers.host` ile doğrudan `new URL(req.url, 'http://'+host)` kuruyor ve handler/global exception guard yok. İzole Node testi `Host: [` isteğinin HTTP parser'dan geçtiğini ve callback içinde `UNCAUGHT:ERR_INVALID_URL` ürettiğini doğruladı. Secure tunnel üzerinden böyle bir malformed Host ulaşırsa Playwright supervisor düşebilir; Bug 80 nedeniyle backend orphan da kalabilir. Çözüm: URL parse try/catch + fixed local base/yalnız pathname extraction, invalid Host için 400; request handler hiçbir girdide process-level exception üretmemeli.


82. [FIXED 2026-09-20 Source Reconciliation] `tests/NativeInstallerSourceRegression.ps1` hedefli çalıştırmada exit 1 verdi; GitHub `windows-ci.yml` bunu zorunlu `Validate source contracts` gate olarak çalıştırıyor. Daha önemlisi `InstallerCanReplaceNonStoppableService` hâlâ `deleting registration before terminating PID` ifadesini başarı kriteri sayıyor ve bugün TRUE dönüyor, çünkü tehlikeli generic `StopAndDeleteServiceAsync` kodu dosyada Cloudflared/gerçek delete senaryoları için hâlâ mevcut; test Talvora upgrade yolunun artık bu metodu çağırmadığını doğrulamıyor. Böylece az önce kökten düzeltilen servis-kaybı bug'ı için regression koruması gerçekte yok. Aynı test yeni mimariye taşınan reconnect/recovery sözleşmelerini eski kaynak-patternlerle aradığı için `TrayReconnectIsNative=False` ve `TrayAutoReconnectsAfterStartup=False` verip CI'ı da gereksiz kırıyor. Çözüm: Talvora upgrade path'inde `StopAndDeleteServiceAsync(ServiceName)` yasaklayan negatif contract + `StopServiceForUpgradeAsync`/`sc config`/early rollback şartlarını doğrulayan test; Tray contract'larını yeni sınıflara göre güncellemek.


83. [FIXED 2026-09-20 Source Reconciliation] `InstallPlaywrightManagedMcpAsync` task XML'ini kurup `/Run` çağrısının yalnız exit code'unu kontrol ediyor; task kabul edilirse npm `@playwright/mcp@latest` resolution, supervisor start, 8931/8932 listen, MCP initialize/tools veya browser smoke beklenmiyor. Node yoksa installer bunu yalnız loglayıp başarıyla devam ediyor; Node mevcut fakat npm/network/CLI/proxy startup bozuksa da installer sonradan exit 0 verebilir. Production hedefi Playwright'ın gerçek çalışır kurulumu olduğundan installer success semantiği eksik. Çözüm: first-run incomplete mode yalnız dependency gerçekten yoksa bilinçli Attention olarak işaretlenmeli; dependency mevcutsa bounded post-task readiness (task running + ports + MCP initialize/tools, browser smoke tercihen Tray startup recovery ile) installer gate olmalı ve başarısızlık rollback/clear diagnostic üretmeli.


84. [FIXED 2026-09-20 Source Reconciliation] `PlaywrightManagedMcpRecoveryDiscovery.ProtocolProbe.RequiredTools` yalnız `browser_tabs`, `browser_navigate`, `browser_snapshot` kontrol ediyor. Canlı 0.0.82 yüzeyi click/type/fill/evaluate/network/screenshot ve etkin `vision,pdf,devtools` yeteneklerine ait çok daha geniş araç seti sunuyor. Core veya özellikle kullanıcı tarafından etkinleştirilen capability araçları kaybolsa/misconfigure olsa ToolCount yüksek kalabilir ve dashboard yine Ready olabilir. Çözüm sürümdeki tüm tool listesini pinlemek değil; stable capability contracts tanımlamak (ör. core interaction + file/upload + screenshot + pdf + devtools temsilci araçları) ve enabled caps metadata ile beklenen seti dinamik doğrulamak.


85. [FIXED 2026-09-20 Source Reconciliation] Launcher startup stale cleanup `supervisorPid` ve `backendPid` için sırayla `Stop-Process -Force` kullanıyor; supervisor önce zorla öldürüldüğü için kendi graceful child cleanup'ını çalıştıramaz, backend zorla öldürüldüğünde de Windows Chrome descendants'ı otomatik sonlandırmaz. Generic Stop da önce `Stop-ScheduledTask` ile launcher'ı bitirip sonra yalnız state'teki launcher PID tree'sini öldürmeye çalışıyor; launcher çoktan yoksa descendants kaçabilir. Persistent profile kullandığımız için orphan Chrome profile lock ve port/restart sorununa yol açabilir. Çözüm: process-tree ownership'i Windows Job Object veya deterministic descendant traversal ile yönetmek; supervisor/backend/Chrome tek atomik kill-tree domain'i olmalı, supervisor graceful stop önce denenmeli.


86. [FIXED 2026-09-20 Source Reconciliation] `ExecuteGenericAsync(Restart)` configured tunnel için önce `DisconnectExistingAsync` çağırıyor ve `tunnel-client runtimes stop <alias>` nonzero ise işlemi kesiyor. İzole v0.0.14 testi bilinmeyen alias için exit 1 / `alias ... is not known; run create or connect first` döndürdü. Auto-recovery Offline durumda Start seçtiği için çoğu kez kaçınıyor, ancak kullanıcı Restart veya stale config+silinmiş runtime state senaryosunda connect aşamasına hiç ulaşmayabilir. Çözüm: stop/disconnect `already absent/not known/not running` durumlarını idempotent başarı kabul etmeli; diğer hatalar korunmalı.


87. [FIXED 2026-09-20 Source Reconciliation] `StopServiceForUpgradeAsync` upgrade öncesi service failure actions'ı `reset=0 actions=` ile geçici temizliyor. Fonksiyon force-stop sonrasında yine STOPPED olamazsa exception atıyor; outer rollback aynı StopServiceForUpgradeAsync'i tekrar çağırıyor ve o da başarısız olursa `CreateServiceAsync`'e ulaşıp restart recovery policy'yi restore edemiyor. Servis kaydı korunur ama automatic SCM recovery actions boş kalabilir. Çözüm: eski failure configuration'ı capture/restore eden finally veya stop başarısız olsa bile recovery policy restore guarantee; transaction sonucu ne olursa olsun servis recovery ayarları canonical durumda kalmalı.


88. [FIXED 2026-09-20 Source Reconciliation] `InstallPlaywrightManagedMcpAsync` mevcut `Start-PlaywrightMcp.ps1` ve `runtime\supervisor.mjs` dosyalarını overwrite ediyor, task'ı `/End` ile durduruyor ve runtime processlerini öldürüyor; sonra yeni task XML oluşturuyor. Bu aşamadan sonra failure olursa outer installer rollback yalnız Talvora service + önceki Tray'i geri yüklüyor; önceki Playwright assets/task definition/runtime state restore edilmiyor. Yeni Playwright payload/task bozuksa başarılı eski Playwright kurulumu kaybedilebilir. Çözüm: Playwright asset/task snapshot + staged files + task XML export; yeni task/readiness GREEN olduktan sonra commit, failure'da eski assets/task ve runtime'ı restore eden transaction.


89. [FIXED 2026-09-20 Source Reconciliation] Installer `ResolvePlaywrightPowerShell()` pwsh 7 yoksa Windows PowerShell 5.1 seçiyor; ancak `Start-PlaywrightMcp.ps1` stale process-state cleanup içinde `([string]$process.CommandLine).Contains(marker,[StringComparison]::OrdinalIgnoreCase)` kullanıyor. PS5.1/.NET Framework bu 2-arg Contains overload'unu desteklemiyor (Bug 65 için izole test MethodException ile kanıtlandı). İlk start'ta process-state yoksa geçebilir, sonraki restart/recovery'de state varsa launcher kırılır. Çözüm: launcher dahil tüm fallback scriptleri PS5.1-compatible `IndexOf(...,[StringComparison]::OrdinalIgnoreCase) -ge 0` kullanmalı ve fallback için ayrı regression test eklenmeli.


90. [FIXED 2026-09-20 Source Reconciliation] Canlı runtime Node `v24.21.0`; resmi Node release/download sayfasında 2026-09-16 itibarıyla en yeni Current `v26.9.0`, v24.21.0 ise LTS'dir. Chrome 153.0.8010.53 ise 2026-09-17 Windows Stable ile eşleşiyor ve `@playwright/mcp` npm latest 0.0.82 ile canlı package eşleşiyor. Kullanıcının `her zaman en son en modern versiyon` kuralı LTS yerine latest Current gerektiriyor; Node bu kurala uymuyor. Çözüm pinlemek değil, canonical dependency bootstrap'ta latest Node Current çözmek/yükseltmek ve runtime preflight'ta latest-check yapmak.


91. [FIXED 2026-09-20 Source Reconciliation] Installer ve launcher Node/npm'i yalnız `%ProgramFiles%\nodejs\node.exe/npm.cmd` altında arıyor. PATH/current-user/custom Chocolatey location'da geçerli latest Node olsa bile kullanmıyor; gerçekten Node yoksa installer task'ı kurup yalnız `first-run incomplete` logluyor, Chocolatey ile latest Current Node sağlamıyor. Bu hem taşınabilirliği hem üretim-grade first-run'ı zayıflatıyor. Çözüm: deterministic command resolution (Talvora resolve/registry/PATH + canonical preferred location), sürüm/preflight kontrolü ve gerektiğinde kullanıcı kuralına uygun Chocolatey `nodejs` latest kurulumu; `nodejs-lts` kullanılmamalı çünkü latest Current politikası var.


92. [FIXED 2026-09-20 Source Reconciliation] `ControlCenterComponentHealthService.GetTunnelStateAsync` state root'taki `<alias>.url` dosyasını okuyup yalnız `/healthz` ve `/readyz` HTTP success status kontrol ediyor. v0.0.14 `runtimes status <alias> --json` ise alias, tunnel_id, runtime_state, process_running/PID, profile path, healthy/ready ve issues bilgisini veriyor. Stale health URL portu başka local process tarafından yeniden kullanılırsa 2xx yanlış Ready üretebilir; ayrıca doğru runtime/tunnel kimliği doğrulanmıyor. Çözüm: bounded structured status probe ile expected alias+tunnelId+process/profile identity doğrulamak; HTTP endpointleri hızlı sinyal olarak korunabilir.


93. [FIXED 2026-09-20 Playwright Compatibility Proxy SSE Header Flush / Tunnel Startup Probe] Kök neden kesinleştirildi: tunnel-client v0.0.14'ün kullandığı modelcontextprotocol/go-sdk v1.7.0 doğrudan 8931 backend'e birkaç ms içinde bağlanırken 8932 Node compatibility proxy üzerinde standalone SSE GET /mcp response header'ları body gelene kadar istemciye flush edilmediği için Client.Connect 2 saniyelik startup probe deadline'ını aşıyordu. Proxy upstream response yolunda res.flushHeaders() uygulanıp backend MCP readiness preflight ile 8932 yalnız gerçek initialize hazır olduktan sonra expose edildi. Exact-installed doğrulama: Go MCP SDK v1.7.0 + standalone SSE açık bağlantı 8932'de 14.6 ms, tunnel /readyz HTTP 200 ready, production smoke 45 tool GREEN ve yeni tunnel loglarında mcp probe timed out / failed to connect to mcp yok. OAuth discovery 404 WARN upstream tunnel-client'ın düz/no-auth MCP için optional discovery davranışıdır ve readiness'i bloklamıyor.


94. [FIXED 2026-09-20 Source Reconciliation] `PlaywrightManagedMcpRecoveryDiscovery.DiscoveryHints` raw-log için yalnız `logs\server.log` ve `logs\bootstrap.log` ekliyor; `McpTunnel\state\logs\playwright-business.log` dahil değil. Control Center Raw Log viewer yalnız TrayLog + `log-file` hints okuduğu için tunnel OAuth discovery, dispatcher, startup probe ve remote-forwarding hataları Playwright ekranında görünmez. Derin audit'te bu log dışarıdan okunarak kritik warnings bulundu. Çözüm: tunnel state root/alias'tan canonical tunnel log hint üretmek ve raw-log viewer'da kaynak etiketini ayırmak.

95. [FIXED 2026-09-20 Source Reconciliation] dotnet list package --outdated --include-transitive doğrudan wildcard PackageReference kayıtlarının güncel olduğunu, fakat resolved transitive graph içinde daha yeni sürümler bulunduğunu gösterdi: Microsoft.Extensions.AI.Abstractions 10.8.3 -> 10.10.0; SQLitePCLRaw 2.1.12 -> 3.x; bazı Microsoft.Extensions abstractions 10.0.10 -> 10.0.12. Bunlar Talvora tarafından pinlenmiyor, parent package constraint/restore graph sonucu. Kullanıcının her şey latest kuralı transitive graph düzeyine de uygulanacaksa compatibility-aware override stratejisi gerekir.

96. [FIXED 2026-09-20 Source Reconciliation] Launcher her task başlangıcında npm install ile @playwright/mcp latest çözümünü zorunlu yapıyor; npm veya network geçici erişilemezse daha önce kurulmuş ve çalışan CLI mevcut olsa bile launcher exit edip Playwright MCP yi tamamen offline bırakıyor. Sürüm pinlemeden çözüm: her startta latest update attempt; registry erişimi başarısızsa mevcut doğrulanmış CLI ile degraded-but-running fallback, Attention/event kaydı ve sonraki recovery döngüsünde retry.

97. [FIXED 2026-09-20 Generic Attention Recovery Preserves Healthy MCP/Browser Chain] Generic Attention remediation reason-aware hale getirildi. Browser smoke eksikse çalışan MCP zinciri üzerinde smoke yenileniyor; tunnel sorunu varsa yalnız mevcut tunnel yeniden bağlanıyor; bu işlemler başarısız olduğunda full restart fallback'ına düşmek yerine zincir korunup retry/backoff uygulanıyor. Canlı fault injection'da browser-smoke marker silinip yeniden üretildi; generation, launcher, supervisor ve backend PID'leri değişmedi.

98. [FIXED 2026-09-20 Source Reconciliation] LoadOrRecoverCoreAsync bozuk veya okunamayan registry durumunda yalnız hard-coded Talvora, Gitea ve Playwright recovery discovery sonuçlarını yeniden yazar. Gelecekte dedicated discovery implementasyonu olmayan generic MCP kayıtları registry corruption halinde kaybolabilir. Mevcut üç canonical MCP etkilenmiyor.

99. [FIXED 2026-09-20 Source Reconciliation] Registry createBackup true ile backup üretebiliyor, fakat primary JSON parse veya validation hatasında backup restore denenmeden hard-coded discovery rebuild yapılıyor. Doğru sıra primary fail, validated backup read/restore, discovery merge; backup da geçersizse son çare discovery rebuild olmalı.

100. [FIXED 2026-09-20 Source Reconciliation] Control Center refresh interval 8 saniye. RefreshDashboardAsync Playwright için MCP initialize, tools list ve browser_tabs list yapıyor; detail açıksa aynı turda RefreshDetailAsync ve GetStatesAsync tekrar protocol probe yapıyor ve task/process component health için PowerShell processleri spawn ediyor. Generic tray recovery de 15 saniyede ayrı probe yapıyor. Kısa ömürlü per-MCP health cache veya dashboard probe sonucunu detail renderer a taşıma gerekli.

101. [FIXED 2026-09-20 Source Reconciliation] Canlı tray.log Gitea endpoint offline iken Gitea version could not be read hatasını yaklaşık her 8 saniyelik Control Center refresh te tekrar yazmış. FileLog bounded olsa da diagnostics gürültüleniyor. Sürekli detail-source failure kayıtları state transition veya bounded interval bazlı dedup/rate-limit/backoff ile loglanmalı.

104. [FIXED 2026-09-20 Source Reconciliation] Faz 12 sırasında daha önce taranmamış `ParseExpectedSha256` kodu compiler gate'te CS1009 verdi; C# normal string içinde regex `\s`/`\*` escape'i yanlış yazılmış. Tunnel-client latest updater bu haliyle kaynak build'i kırıyordu. Raw/verbatim regex ile düzeltilecek ve targeted Tray build ile doğrulanacak.


105. [FIXED 2026-09-20 Source Reconciliation] Faz 12 transaction reordering sırasında `CleanupObsoleteInstallationsAsync` Playwright final readiness öncesinde kalırsa önceki version-root rollback binary'si silinebilir; ardından Playwright failure outer rollback'i yapamaz. Düzeltme: Playwright transactional commit önce, obsolete cleanup sonrasında best-effort/non-fatal çalışacak; önceki version rollback kaynağı tüm failure-prone aşamalar bitene kadar korunacak.


106. [FIXED HIGH 2026-09-20 npm Latest Verification Uses Interactive Playwright User Context] npm@latest install/view/version/prefix işlemleri installer SYSTEM context'inden çıkarılıp Playwright scheduled task ile aynı interactive install-user context'inde çalıştırılıyor. Gate, npm prefix -g değerini installUser.RoamingAppData\npm ile doğruluyor. Exact-installed canonical deploy npm=12.0.2 ve npmPrefix=C:\Users\tayla\AppData\Roaming\npm ile GREEN; server-start.json da npmVersion=12.0.2 raporluyor.

107. [FIXED HIGH 2026-09-20 Dashboard Non-Smoke Probe No Longer Self-Invalidates Browser Smoke] Non-smoke dashboard/detail health artık browser_tabs çağırmıyor ve browser process başlatmıyor. Gerçek navigate+ARIA smoke sırasında canlı Chrome PID/start-time hâlâ doğrulanıp marker'a yazılıyor; marker geçerliliği ise current Playwright runtime generation'a bağlandı. Canonical deploy dirty-d504e2d57553 sonrası generation=45ecffa5c5fd4e3c95967722ce10fd15 ile smoke marker generation birebir eşleşti; 54+ saniyelik polling boyunca Chrome gereksiz yere açılmadı ve yeni Automatic managed MCP recovery failed / Browser doğrulaması hatası oluşmadı.


108. [FIXED 2026-09-20 Official Playwright CLI Migration] Mimari kural ihlali: APPLICATION-DEVELOPMENT-ROADMAP açıkça "Do not add Playwright to Talvora" diyor; buna rağmen installer, Tray managed registry, scheduled task, compatibility proxy, Secure MCP Tunnel ve production smoke zinciri Playwright MCP'yi Talvora içine gömmüş durumda. Microsoft'un güncel resmi yönlendirmesinde @playwright/cli coding-agent kullanımı için token-verimli birinci sınıf yol. Karar: ayrı Playwright MCP server/tunnel/supervisor/scheduled-task zincirini kaldır; Playwright CLI'yı Talvora'dan bağımsız current-user global araç olarak @playwright/cli@latest kur; mevcut browser profile verisini bağımsız Playwright CLI profile alanına güvenli biçimde taşı; Talvora installer/Tray/registry/smoke/build payload'ından Playwright MCP sahipliğini tamamen çıkar; live artıklarını temizle ve targeted CLI smoke + Talvora build/deploy ile doğrula.


109. [FIXED 2026-09-20 Canonical Deploy Silent SYSTEM Task] `scripts\Deploy-Windows-Installer.ps1` bağımsız SYSTEM Task Scheduler action'ında `Talvora-Setup.exe` için argüman vermiyor. Installer yalnız `--silent` ile unattended akışa giriyor; Session 0 SYSTEM task altında GUI yolu görünmeden bekliyor, install.log'a yeni `Install started` yazılmıyor ve canonical deploy task sonsuza kadar Running kalabiliyor. Çözüm: task action'a explicit `--silent` ekle, source regression ile zorunlu kıl ve takılı instance'ı temizleyip canonical deploy'u yeniden doğrula.


2026-09-20 #108 resolution: official `@playwright/cli@latest` current-user global kurulumunda `playwright-cli 0.1.21` doğrulandı. Persistent profil `C:\Users\tayla\AppData\Local\PlaywrightCLI\profile` konumuna taşındı. Full representative CLI smoke navigate/snapshot/find/run-code/network/console/tabs/tracing/screenshot/PDF GREEN; eski Talvora Playwright MCP task/root/process/tunnel/proxy/listener kalıntıları kaldırıldı. Canlı registry/recovery/ownership yalnız `gitea` + `talvora` içeriyor; 8931/8932 listener yok; global @playwright scope yalnız `cli` içeriyor. Installer/Tray/build payload/recovery/smoke kaynaklarından Playwright MCP sahipliği kaldırıldı ve exact-installed runtime `747cbfc560c8f7e9d4c3def699ee986d5c164a71-dirty-ce803e6e4f95` olarak deploy edildi.

2026-09-20 #109 resolution: `Deploy-Windows-Installer.ps1` Task Scheduler action artık explicit `--silent` geçiriyor. Windows PowerShell 5.1 parse ve NativeInstaller source regression GREEN. Önceki takılı Session 0 GUI instance temizlendi; düzeltilmiş canonical deploy `LastTaskResult=0`, installer process count=0 ve install.log `Install succeeded` ile doğrulandı.


110. [FIXED 2026-09-20 Canonical Source Edit Transaction Engine / Legacy Mutation Guard] Agent-first Source Edit Transaction Engine production core olarak uygulandı: `talvora_read_source` revision handshake, exact-by-default Talvora Patch DSL `talvora_apply_patch`, deterministic `talvora_apply_edits`, normalized changeset core, SHA-256 optimistic concurrency, transactionId/requestHash idempotency, durable WAL/receipt, same-volume stage/backup, rollback/crash recovery, stable domain errors, encoding/BOM/newline preservation, source classification ve server-side SourceMutationPolicy. Git unified diff core değildir; fuzzy-only mutation yoktur. Structural ast-grep (#114) ve Roslyn semantic (#120) adapter'ları sonradan aynı transaction çekirdeğine bağlanan ayrı specialist capability boundary olarak uygulanmıştır. Ayrıntılı sözleşme: `SOURCE-EDIT-ENGINE-ARCHITECTURE.md`.


111. [FIXED 2026-09-20 Source Edit Revision Handshake] `talvora_read_source` text + SHA-256 revision + encoding/BOM/newline/workspace classification döndürüyor; existing-file apply_patch/apply_edits çağrıları revision zorunluluğu ile stale read'i mutation öncesi reddediyor.

112. [FIXED 2026-09-20 Multi-file Windows Atomicity / Commit-race Semantics] TxF kullanılmadan durable append-only WAL, same-volume deterministic staging/backup, Flush(true), File.Replace/ReplaceFileW displaced-original hash verification, MoveFileExW WRITE_THROUGH, commit barrier, reverse rollback ve crash recovery uygulandı. Semantik recoverable all-or-nothing transaction olarak tanımlanıyor; olmayan bir Windows multi-file atomic syscall iddia edilmiyor.

113. [FIXED 2026-09-20 Legacy Source Mutation Policy False-positive Risk] SourceWorkspaceClassifier nearest development-workspace markerları + path containment + known source/config/doc names + text/binary sniffing + generated/cache classification kullanıyor. Legacy guard yalnız recognized workspace source/text hedeflerinde uygulanıyor; live smoke repo-dışı ordinary write compatibility'nin korunduğunu doğruladı.

114. [FIXED 2026-09-20 Structural Backend Provisioning / Transaction Boundary] ast-grep structural adapter commercial-quality boundary ile exact-installed tamamlandı. Chocolatey'de paket hâlâ yok; canonical build resmi platform-specific npm paketinin latest sürümünü çözüyor, MIT + SHA-512 SRI metadata'sını doğruluyor, install scripts kapalı staging kullanıyor ve yalnız native `ast-grep.exe` + provenance'i versioned Service payload'a gömüyor. Runtime vendored executable'ın version + SHA-256 provenance'ini doğrulayıp onu tercih ediyor. `talvora_structural_edit` yalnız isolated UTF-8 mirror üzerinde `--json=stream` proposal üretir; `--update-all` / `--interactive` canonical path'te yoktur. Pattern/kind+rewrite ve YAML rule/fix modları desteklenir. Her proposal Unicode-scalar position + UTF-8 byte offset + exact matched text/replacement offsets ile çapraz doğrulanır, sonra expectedRevision/ExpectedText taşıyan exact structured changes olarak mevcut WAL/rollback/idempotency engine'ine girer. Exact-installed evidence: sourceCommit `747cbfc560c8f7e9d4c3def699ee986d5c164a71-dirty-b55f88d972ca`, raw MCP tools/list 203, ast-grep 0.45.3, executable SHA-256 `DAFF0F5963FAAB7617045833132A3538C85EEE65F3AFEEDF347F829A7B8D83FB`, live emoji-containing TypeScript pattern/rewrite GREEN, live YAML rule/fix GREEN, same structural transaction retry `replayed=true`. Source Edit regression 27/27, NativeInstaller source regression GREEN, Talvora Release 0 warning / 0 error.



115. [FIXED 2026-09-20 Source Edit Lost-Response Idempotency Lookup Ordering] requestHash yalnız external request + canonical workspace kimliğinden mutation/preflight'tan önce hesaplanıyor; durable receipt/journal lookup live normalization'dan önce çalışıyor. Dedicated committed-but-response-lost regression ve live aynı-transaction retry `replayed=true` GREEN.



116. [FIXED 2026-09-20 Commit-time Concurrent Writer Rollback Ownership] attempted/applied commit-step ownership izleniyor; normal failure/cancellation rollback yalnız Talvora'nın gerçekten uyguladığı adımları geri alıyor. Backup/target before-after hash kanıtı yoksa dış süreç verisi ezilmiyor ve RecoveryRequired oluşuyor. Commit-barrier sonrası concurrent-writer regression GREEN.



117. [FIXED 2026-09-20 Typed Config Mutation Source-Edit Policy Coverage] JSON/dotenv/INI/XML/YAML/TOML set/delete typed mutator'ları da recognized development workspace source/config hedeflerinde server-side SourceMutationPolicy ile engelleniyor; tool descriptions PRIMARY/default `talvora_apply_patch` ve exact-range-only `talvora_apply_edits` ayrımını taşıyor. Gerçek typed-tool regression ve live JSON policy smoke GREEN; repo-dışı compatibility korunuyor.

118. [FIXED 2026-09-20 MCP Domain Error Visibility] Güncel resmi MCP C# SDK davranışına uygun olarak SourceEditDomainException `ModelContextProtocol.McpException` türevi yapıldı. Exact-installed bağımsız MCP client smoke artık policy reddinde `SOURCE_EDIT_POLICY_VIOLATION` domain kodunu görüyor; generik invocation error'a düşmüyor.

119. [FIXED 2026-09-20 Agent Source-Edit Routing Determinism / File Lifecycle Coverage Gap] 201-tool katalogda benzer mutator yüzeyleri agent'ı deneme-yanılmaya itebiliyordu ve generic `talvora_delete` / same-workspace `talvora_move` source-file lifecycle işlemleri WAL/revision/rollback çekirdeğinin dışında çalışabiliyordu. Routing Contract v2 ile `talvora_apply_patch` normal source/config/repo-doc ve one-or-many-file editlerin PRIMARY/default aracı olarak sabitlendi; `talvora_apply_edits` yalnız exact UTF-16 range/revision zaten biliniyor/üretiliyorsa specialist olarak seçiliyor. MCP `ServerInstructions`, tool Title/Description, read-only `talvora_source_edit_guide`, `SOURCE_EDIT_POLICY_VIOLATION` ve regression aynı merkezi sözleşmeyi taşıyor. Direct guarded source-file delete ve same-workspace move canonical engine'e yönlendiriliyor; directory/generated/binary/non-workspace ve cross-workspace filesystem capability ile unrestricted PowerShell/process korunuyor. Unified-diff copy compatibility fallback'i de başka editöre değil aynı `talvora_apply_patch` Talvora DSL girişine yönlendiriliyor.

2026-09-20 #119 resolution evidence: Talvora Release build 0 warning / 0 error; Source Edit targeted regression 25/25 GREEN; ambiguous `use talvora_apply_patch or talvora_apply_edits` source-description taraması 0 match. Pre-doc exact-installed routing gate 202 tool + 0 duplicate gördü; `talvora_source_edit_guide` structured content doğru PRIMARY/default rotayı döndürdü. Live workspace source delete ve same-workspace move `SOURCE_EDIT_POLICY_VIOLATION` ile reddedildi; aynı kaynak cross-workspace move ile başarıyla taşınabildi. Live iki-dosyalı ordinary change tek `talvora_apply_patch` transaction'ında commit oldu; aynı transaction retry `replayed=true` döndü. Final dirty `sourceCommit` docs-only canonical redeploy sonunda live doğrulanır; repo içi self-referential fingerprint kaydı tutulmaz.

2026-09-20 #110/#111/#112/#113/#115/#116/#117/#118 baseline evidence: Önceki exact-installed runtime `747cbfc560c8f7e9d4c3def699ee986d5c164a71-dirty-362111fa4d0d` üzerinde bağımsız MCP client tools/list 201 tool gördü ve `talvora_read_source`, exact patch, structured edits, same-transaction idempotent replay, legacy workspace rejection, typed JSON rejection ve non-workspace compatibility live smoke'u `TALVORA LIVE SOURCE EDIT SMOKE GREEN` verdi. Routing Contract v2 / #119 bu baseline'ı yukarıdaki 202-tool exact-installed doğrulamayla genişletir.

120. [FIXED 2026-09-20 Roslyn C# Semantic Edit / Routing Contract v3] C# symbol identity gerektiren işlemler için dar `talvora_semantic_edit` specialist katmanı uygulandı; ordinary C# source mutation PRIMARY/default `talvora_apply_patch` üzerinde kaldı. Current capability solution/project-aware symbol rename'dir. `Microsoft.Build.Locator` deterministic olarak workspace creation öncesi register edilir; disposable `MSBuildWorkspace`, current `RegisterWorkspaceFailedHandler`, SemanticModel/SymbolFinder ve current `Renamer.RenameSymbolAsync(..., SymbolRenameOptions, ...)` kullanılır. Missing/ambiguous symbol, linked multi-project identity conflict, stale source revision ve workspace load failure mutation öncesi domain error üretir. Roslyn live workspace'e yazmaz; changed Solution yalnız exact revisioned UTF-16 proposals üretir ve commit `SourceEditEngine.ApplyGeneratedEditsAsync` üzerinden mevcut SHA-256/WAL/idempotency/rollback/recovery çekirdeğine girer. Formatter/Simplifier otomatik çalıştırılmaz; encoding/BOM/newline ve unrelated formatting korunur.

2026-09-20 #120 resolution evidence: Roslyn Workspaces 5.9.0, Microsoft.Build.Locator 1.11.2 ve compile-only Microsoft.Build.Framework 17.14.28 resolved; Source Edit/Semantic targeted regression 33/33 GREEN; Talvora Release 0 warning / 0 error; NativeInstaller source regression GREEN; `git diff --check` exit 0; current canonical installer SHA-256 `28171A926EA96D78B05724201BCAFA30E3351208DB56EAB517384FFB0AFC991A`; canonical deploy/live snapshot `sourceCommit=747cbfc560c8f7e9d4c3def699ee986d5c164a71-dirty-8726cde50619`. Raw exact-installed `tools/list` 204/204 unique ve routing metadata PRIMARY patch / structural specialist / semantic specialist / exact-range specialist ayrımını doğru döndürdü. Latest real two-project live semantic smoke `LiveSmoke.Widget -> RenamedWidget` yaptı, declaration UTF-16 LE BOM ve declaration/reference CRLF + deliberate spacing korunarak semantic references değişti; aynı semantic transaction retry `replayed=true` döndü. Bu evidence snapshot'ından sonra yalnız yaşayan docs closeout yapılır; repo içinde self-referential docs-only fingerprint kovalanmaz.

121. [FIXED 2026-09-20 Semantic Project Scope Completeness] `.csproj` semantic input artık workspace içindeki solution/project topolojisini bounded ve reparse-free discovery ile değerlendiriyor. Target project'i içeren tek `.sln/.slnx` bulunduğunda işlem otomatik olarak full-solution scope'a yükseliyor ve receipt gerçek solution path/kind/project count'u raporluyor. Birden fazla containing solution varsa `SEMANTIC_SCOPE_AMBIGUOUS`; multi-project workspace'te unique containing solution yoksa reverse-dependent completeness kanıtlanamadığı için `SEMANTIC_SCOPE_INCOMPLETE` ile mutation öncesi reddediliyor. Tek-project workspace'te project-mode korunuyor.

122. [FIXED 2026-09-20 Baseline Compilation Errors Are Visible and Fail Closed] Loaded C# projects için `Project.GetCompilationAsync` + `Compilation.GetDiagnostics(cancellationToken)` baseline gate'i eklendi. Suppressed olmayan compiler Error diagnostics structured `csharp-compiler` receipt'lerine code/message/project/document bilgisiyle giriyor; baseline error varsa `SEMANTIC_COMPILATION_INVALID` ile rename proposal üretilmeden zero-mutation reddediliyor.

123. [FIXED 2026-09-20 Rename Proposal Compiler Correctness Gate] Roslyn changed Solution üzerinde aynı compiler Error gate'i commit conversion öncesinde çalışıyor. Rename proposal compiler error üretirse structured diagnostics korunarak `SEMANTIC_CONFLICT` dönüyor ve Source Edit transaction'a mutation proposal verilmeden işlem bitiyor.

2026-09-20 #121/#122/#123 remediation evidence: current Roslyn docs `Renamer.RenameSymbolAsync` kapsamının loaded `Solution` olduğunu, `Project.GetCompilationAsync` ve `Compilation.GetDiagnostics` compiler diagnostic yüzeyini doğruluyor. Targeted fixtures: project input + unique `Demo.slnx` iki-project scope'a otomatik yükseldi ve reverse consumer referansları değişti; solution'sız multi-project scope zero-mutation `SEMANTIC_SCOPE_INCOMPLETE` verdi; missing HintPath/type fixture `SEMANTIC_COMPILATION_INVALID` + compiler error diagnostics verdi; `Alpha -> Beta` collision `SEMANTIC_CONFLICT` + `CS0101` verdi ve source değişmedi. Full Source Edit targeted regression **46/46 GREEN**.

124. [FIXED 2026-09-20 Semantic Durable Replay Preserves Adapter Receipt Metadata Through WAL Recovery] Source Edit transaction sonucu artık public MCP payload'ına sızdırılmayan internal adapter receipt metadata taşıyabiliyor; store aynı metadata'yı backward-compatible persisted receipt şekline ve checksum-protected `Prepared` WAL planına bağlıyor. `ApplyGeneratedEditsAsync` semantic generator tamamlandıktan sonra immutable adapter receipt'i capture ediyor; rejection receipt'leri de mevcut semantic context'i durable saklıyor. Commit receipt kaybolursa `BuildPlanResult` metadata'yı WAL planından yeniden kuruyor. Roslyn semantic rename adapter'ı schema-versioned `semantic-rename` receipt içinde workspace/MSBuild, symbol identity ve bounded diagnostics alanlarını serialize ediyor ve normal replay/recovery response'unu bu durable receipt'ten üretiyor. Updated `semantic-solution-rename-replay` regression ilk başarılı rename sonrası receipt dosyasını bilinçli siliyor; aynı transaction Committed WAL'dan recovery ile yeniden oynatıldığında workspace/symbol/diagnostics birebir korunuyor ve receipt yeniden oluşturuluyor. Source Edit regression **60/60 GREEN**; regression Release build **0 warning / 0 error**.

125. [FIXED 2026-09-20 Nested Project Move Uses Destination/Source Classification Instead of Nearest-Root Equality] Generic file move artık nearest workspace-root equality ile karar vermiyor. Default policy source ve destination'ı ayrı sınıflandırıyor; development-workspace source removal veya source destination varsa `SOURCE_EDIT_POLICY_VIOLATION` ile `talvora_apply_patch` rotasına dönüyor. Böylece aynı Git repository içindeki nested `App/*.csproj -> Lib/*.csproj` move ayrı nearest project root'ları seçse bile canonical lifecycle dışında çalışmıyor. `explicitAdmin=true` deliberate administrative move yeteneğini koruyor.

126. [FIXED 2026-09-20 Generic Copy Source-Destination Routing Guard] `talvora_copy` file destination'ını, recursive directory copy ise manifestteki bütün file targets'i live mutation başlamadan önce SourceMutationPolicy ile değerlendiriyor. Development-workspace source/text destination defaultta `talvora_apply_patch` rotasına dönüyor; directory copy policy failure destination tree oluşmadan gerçekleşiyor. Routing guide Avoid listesi ve tool description aynı kararı taşıyor. Binary/generated/non-workspace copy ile `explicitAdmin=true` administrative source copy kapasitesi korunuyor.

127. [FIXED 2026-09-20 Structural YAML Rule Self-Scan / Control-File Collision] Rule-file mode workspace'i UTF-8 mirror'a kopyaladıktan sonra aynı mirror root'a `.talvora-structural-rule.yml` control file yazıyor ve `ast-grep scan --rule <control> ... .` çalıştırıyor. Valid `language: Yaml`, `pattern: foo`, `fix: bar` rule + `data.yml: value: foo` live probe'unda ast-grep control rule dosyasını da match olarak döndürdü; adapter snapshot'ta bulunmayan `.talvora-structural-rule.yml` nedeniyle `STRUCTURAL_PROPOSAL_INVALID` ile zero-mutation reddetti. Güvenli ama normal valid rule çalışmasını bozuyor. Fix yönü: control rule mirror scan ağacının dışında tutulmalı veya explicit exclude uygulanmalı; workspace rule source'unun transform target olup olmayacağı ayrıca açık policy olmalı.
  - Düzeltme/doğrulama: Yardımcı kural staging kökünde, taranan source ağacının dışında tutuluyor; aktif ruleFile dönüşüm hedeflerinden çıkarılıyor. Gerçek ast-grep testi data.yml ile aynı adlı kullanıcı .talvora-structural-rule.yml dosyasını doğru dönüştürdü; rule.yml değişmedi. Önce başarısız/sonra başarılı regresyon ve Source Edit 56/56 GREEN.

128. [OPEN MEDIUM 2026-09-20 Semantic Resource Bounds Apply Too Late] `maxProjects/maxDocuments` ancak `OpenSolutionAsync/OpenProjectAsync` tüm workspace'i yükledikten sonra kontrol ediliyor; `maxChangedDocuments/maxTotalChangedCharacters` Roslyn tüm changed Solution'ı ürettikten sonra kontrol ediliyor. Workspace failure callback'i de load sırasında unbounded `ConcurrentQueue<WorkspaceDiagnostic>` dolduruyor; `maxDiagnostics` yalnız sonradan drain edilirken uygulanıyor. Böylece advertised bounded-state contract kötü/çok büyük solution'da allocation ve CPU'yu mutation öncesi gerçekten sınırlamıyor. Fix yönü: load progress/cancellation üzerinden hard budgets, bounded diagnostic sink ve server-side absolute caps; mümkünse project/document budget aşımında workspace load erken kesilmeli.

129. [OPEN MEDIUM 2026-09-20 Semantic Graph Snapshot Is Not Fully Concurrency-Versioned] Semantic request yalnız anchor source revision'ını dışarıdan taşıyor; solution/project files ve rename sonucunda değişmeyen fakat semantic graph/reference setini etkileyen source documents transaction identity/revision setine dahil değil. Changed documents commit öncesi yeniden snapshot edilse de Roslyn load sonrasında dış writer yeni bir reference ekler veya project graph'ı değiştirirse yeni target set gözden kaçabilir ve partial semantic rename başarılı commit olabilir. Fix yönü: semantic graph load snapshot'ının solution/project + relevant document revisions/fingerprint'ini capture et, proposal sonrası commit öncesi graph fingerprint'i yeniden doğrula; target-set değişimi zero mutation stale-graph error olmalı.

130. [OPEN MEDIUM DESIGN RISK 2026-09-20 Process-Wide MSBuild Instance Is Not Workspace-Scoped] `RoslynMsBuildBootstrap` ilk semantic çağrıda process-wide tek `MSBuildLocator.RegisterInstance` yapıyor ve sonraki tüm repositories aynı instance'ı kullanıyor. Current machine'de yalnız .NET SDK 10.0.401 olduğu için multi-SDK live repro yapılamadı; ancak .NET `global.json` workspace'e göre SDK selection/roll-forward tanımlar ve MSBuild SDK resolver arama başlangıcı solution/project konumuna bağlıdır. Talvora'nın highest/discovered-instance seçimini process lifetime boyunca sabitlemesi multi-repo/global.json veya legacy VS-MSBuild senaryolarında workspace toolchain fidelity riski yaratır. Fix yönü: supported toolchain matrix'i netleştir; per-workspace requested SDK/MSBuild identity'yi resolve/validate et ve process-wide Locator kısıtı nedeniyle gerekirse isolated build-host/process kullan.

2026-09-20 deep audit note: #121/#122/#123/#124/#125/#126/#127 live temporary fixtures ile exact-installed `...-dirty-8726cde50619` runtime üzerinde yeniden üretildi. #128/#129 code-path concurrency/resource audit bulgusudur; #130 current official MSBuild/.NET SDK selection semantics ile code design karşılaştırmasından çıkan multi-toolchain design riskidir. NuGet `--vulnerable --include-transitive` taraması current Talvora project için bilinen vulnerable package raporlamadı. Bu audit sırasında production source code değiştirilmedi; yalnız BUG-AUDIT yaşayan envanteri güncellendi.

131. [FIXED 2026-09-20 Generic Mutation Routing Contract / Explicit Admin Override] Central SourceMutationPolicy artık ordinary development-source destination/removal intent'ini copy/move/archive/download ve bilinen Git working-tree mutation commands için defaultta canonical Source Edit'e yönlendiriyor. Archive extraction bütün target file'ları önce planlayıp policy preflight'i mutation öncesi bitiriyor; HTTP download destination policy'si directory creation ve network request'ten önce çalışıyor; Git `restore/reset/checkout/switch/rm/mv/merge/rebase/cherry-pick/apply/am/clean/pull/revert/stash` gibi working-tree değiştiren direct commands recognized development workspace'te default policy'ye bağlı. Read/status/history/fetch/push doğrudan kalıyor. Generic tool'larda `explicitAdmin=true`, PowerShell/process'te mevcut unrestricted administration davranışı korunarak TAM YETKI kırpılmadı.

132. [FIXED 2026-09-20 Playwright MCP Retirement Has Ownership-Aware Installer Upgrade Cleanup] Installer post-commit legacy retirement artık eski `%LOCALAPPDATA%\Talvora\PlaywrightMCP` ağacını bir kerelik migration olarak temizliyor. Aynı adlı scheduled task yalnız XML URI + eski launcher path + normalized WorkingDirectory birlikte eski Talvora task ownership'ini kanıtlarsa End/Delete ediliyor; aynı isimli fakat ownership'i kanıtlanamayan task korunuyor. Process cleanup port numarasına göre global kill yapmıyor: eski launcher/runtime/profile path marker'larını taşıyan processleri ve onların descendant tree'sini hedefliyor. Legacy root silinince içindeki local MCP/tunnel config/state/log/profile/runtime kalıntıları birlikte emekliye ayrılıyor; remote control-plane tunnel admin credential olmadan installer tarafından silinmiyor. Migration tamamlanınca durable `playwright-mcp-retired-v1.json` marker yazılıyor ve tekrar çalışmada idempotent oluyor. `RetiredPlaywrightMcpHasOwnershipAwareUpgradeCleanup` ve güncellenmiş aktif-Playwright-yokluğu source contracts GREEN; Installer Release compile geçici build-only Payload.zip fixture ile **0 warning / 0 error**, fixture sonrasında silindi.

133. [FIXED 2026-09-20 Canonical Deploy WaitForCompletion Is Bound to the Launched Task Instance] `Deploy-Windows-Installer.ps1` artık `Run()` öncesi `LastRunTime` baseline'ını alıyor, `Run()` tarafından döndürülen yeni task instance GUID'ini kaydediyor ve yalnız bu GUID'in running koleksiyonundaki durumunu izliyor. Başarı için artık bir poll turunda running instance görme şartı yok: launched GUID daha önce görüldüyse veya registered task `LastRunTime` baseline'dan ilerlediyse ve aynı instance artık görünmüyor + task terminal state'teyse completion bu run'a bağlanıyor; ardından `LastTaskResult` doğrulanıyor. PowerShell parse GREEN, `CanonicalDeployObservesFastTaskCompletion` source contract GREEN. Gerçek Task Scheduler regression probe ilk poll'u bilinçli olarak 750 ms geciktirdi; fast SYSTEM task bu sırada tamamen bitip `instanceObservedAfterForcedDelay=false` kaldığı halde `lastRunTransitioned=true`, `LastTaskResult=0` ve `FAST_TASK_COMPLETION_GREEN` elde edildi.

134. [FIXED 2026-09-20 Collision-Proof Managed MCP Persistent Identity] Per-entry recovery ve ownership manifest filename kimliği artık lossy character replacement yerine ortak `ManagedMcpIdentityKey` SHA-256 canonical key kullanıyor. Healthy-primary recovery fast-path de `EnsureRecoveryManifestsAsync` ile manifests'i yeni key formuna reseed edip eski stale adları temizliyor. Isolated registry contract fixture `foo/bar` ve `foo?bar` kayıtlarını aynı anda yazıp primary/backup kaldırıldıktan sonra iki kaydın da recovery manifests'ten geri geldiğini doğruladı. Operation coordinator named semaphore da aynı canonical key'i kullanıyor. Talvora.Shared ve Tray Release build 0 warning / 0 error.

135. [FIXED 2026-09-20 validateSyntax=true Is a No-op for C# Ordinary Source Edits] `SourceEditSyntaxValidator` yalnız JSON/JSONC, XML/project, YAML ve TOML validator'larına sahip; `.cs` için validator yok. Roslyn packages artık runtime'da mevcut olmasına rağmen ordinary `talvora_apply_patch validateSyntax=true` açıkça hatalı `public class Demo { public int X => ; }` kaynağını `success=true/status=committed` ile yazdı ve validation `validator=none,status=not-applicable` döndürdü. Bu davranış parametre adının verdiği güvenceyle uyuşmuyor. Fix yönü: en az C# için Roslyn syntax-tree error diagnostics'i transaction preflight gate'ine ekle; desteklenmeyen dillerde API/receipt “syntax validation unavailable” durumunu daha görünür biçimde ayırsın.
  - Düzeltme/doğrulama: Roslyn CSharpSyntaxTree ile .cs ve .csx syntax Error diagnostics preflight aşamasında reddediliyor; tanı çıktısı 20 kayıtla sınırlı. Hatalı C# içeren çok dosyalı patch hiçbir dosyayı değiştirmiyor; geçerli syntax, script ve validateSyntax=false korundu. Bu gate proje derlemesi/type checking değildir. Plain-text transaction test örnekleri .txt uzantısına düzeltildi; Source Edit 56/56 GREEN.

136. [FIXED 2026-09-20 Installer Legacy Cloudflared Cleanup Is Ownership-Aware and Post-Commit] Legacy retirement artık kurulumun service/state/config başarı zinciri tamamlandıktan sonra best-effort post-commit aşamasında çalışıyor. Generic `ProgramData\cloudflared` hiçbir sahiplik kanıtı olmadığı için korunuyor. `Cloudflared` servisi yalnız registry `ImagePath` içinden ayrıştırılan gerçek service executable yolu normalize edilip eski `Program Files\Talvora\Cloudflare\cloudflared.exe` yolu ile exact case-insensitive eşleşirse stop/delete ediliyor; binary-path argümanlarında Talvora yolunun yalnızca geçmesi ownership sayılmıyor. Legacy Talvora Cloudflare root'u da yalnız owned service veya eski Talvora executable varlığı kanıtıyla temizleniyor. Targeted `NativeInstallerSourceRegression.ps1` GREEN; değişen C# dosyaları Roslyn syntax preflight GREEN. Direct Installer project build kaynak hatası olmadan canonical payload'ın bu anda üretilmemiş olması nedeniyle beklenen `CS1566 Payload.zip missing` noktasında durdu.

137. [FIXED 2026-09-20 ast-grep Private Cache Reuse Does Not Cryptographically Reverify Executable] Fresh runtime toolchain install official npm `/latest` metadata + HTTPS tarball + SHA-512 SRI ile güçlü doğrulanıyor; vendored installer payload ayrıca executable SHA-256 doğruluyor. Fakat private ProgramData cache reuse'unun **iki** yolu da binary hash doğrulamıyor: metadata refresh başarılıyken `TryResolveInstalledAsync(root, latest.Version,...)` mevcut `ast-grep.exe` için yalnız `--version` çıktısını kontrol edip latest integrity string'ini response'a taşıyor; metadata refresh başarısızken `TryResolveNewestCachedAsync` yine yalnız `--version` + optional `toolchain.json` integrity text'i ile kabul ediyor. Cache manifestinde executable SHA-256 yok ve stored package SRI executable'a yeniden uygulanamaz. Aynı expected version string'ini veren değiştirilmiş cached binary online veya offline reuse yolunda kabul edilebilir. Fix yönü: install sırasında executable SHA-256 + exact package/version/SRI kaydını cache manifestine yaz; **her** private-cache resolve'da fixed-time SHA-256 + version doğrula, mismatch/legacy unverifiable cache'i quarantine/delete edip verified reinstall/fallback yap.
  - Düzeltme/doğrulama: Her private-cache resolve ortak paket/sürüm/SHA-512 SRI metadata doğrulamasından ve executable SHA-256 karşılaştırmasından geçiyor; executable ancak bundan sonra --version ile çalıştırılıyor. Fresh install executableSha256 kaydediyor. Eksik/eski/bozuk manifest ve hash/kimlik uyumsuzluğu cache'i reddediyor; caller cancellation sağlam cache'i silmeden yayılıyor. Online/offline regresyonlar RED -> GREEN, bozuk JSON/shape ve eksik provenance ek kontrolleri GREEN.

138. [FIXED 2026-09-20 Installer ast-grep Provenance Uses Multiple Mutable @latest Metadata Reads] Canonical build `Install-AstGrepPayload` version, license ve `dist.integrity` alanlarını üç ayrı `npm view <package>@latest <field>` çağrısıyla çözüyor, sonra ilk çözülen `version` ile exact package install ediyor. npm docs exact `package@version` metadata sorgusunu destekliyor. `latest` tag bu üç çağrı arasında değişirse version A kurulurken license/integrity B provenance'e yazılabilir. npm install kendi registry integrity doğrulamasını yapsa bile Talvora'nın kaydettiği provenance snapshot internally inconsistent olabilir. Fix yönü: tek JSON metadata snapshot al veya önce version'ı çöz, sonra bütün provenance alanlarını exact `package@<version>` üzerinden çöz/doğrula.
  - Düzeltme/doğrulama: Install-AstGrepPayload tüm name/version/license/dist alanlarını tek npm view package@latest --json yanıtından alıyor; kurulum o yanıttaki exact version'a bağlanıyor. tests/InstallerAstGrepMetadataRegression.ps1 gerçek fonksiyonu npm test-double ile çalıştırıyor: eski kodda 3 metadata çağrısıyla RED, düzeltmeden sonra valid/missing-integrity/wrong-package 3/3 GREEN. NativeInstallerSourceRegression GREEN; tam installer build/live deploy bu oturumda yapılmadı.

139. [FIXED 2026-09-20 Installer Rollback Restores Post-Health User State] Yeni service health geçtikten sonra herhangi bir kullanıcı state mutation'ı yapılmadan önce installer artık `current.json`, `.codex/config.toml` ve Tray Run registry değerinin exact rollback snapshot'ını alıyor. Dosyalarda önceki existence + byte content + attributes; registry'de existence + raw string + `RegistryValueKind` korunuyor. Service switch sonrası hata oluşursa önce eski service geri başlatılıyor, ardından snapshot `CancellationToken.None` ile restore ediliyor: önceden var olan dosyalar same-directory durable temp + `Flush(true)` + overwrite publish ile byte-for-byte geri geliyor, önceden olmayan ve yeni kurulumun oluşturduğu dosyalar siliniyor; Tray Run değeri de eski exact value/kind'e dönüyor veya önceden yoksa kaldırılıyor. Eski Tray process'i ayrıca yeniden başlatılıyor; executable path'ten registry değerini yeniden üretme kaldırıldı. Dedicated `InstallerRollbackStateRegression.ps1` bütün rollback kontratlarını GREEN doğruladı; geçici build-only `Payload.zip` fixture ile Installer Release build **0 warning / 0 error**, fixture sonrasında silindi.

140. [FIXED 2026-09-20 Canonical Deploy Pins the Built Installer Artifact] Canonical build final `Talvora-Setup.exe` için atomik sidecar `Talvora-Setup.manifest.json` yayımlıyor; manifest exact file name, byte size, SHA-256, source commit, snapshot HEAD, index tree, runtime-input aggregate SHA-256 ve runtime identifier taşıyor. Deploy manifesti zorunlu olarak parse/validate ediyor, canonical build ile aynı `Global\Talvora.BuildWindowsInstaller.v2` mutex'ini non-blocking alarak build sürerken mutable artifact tüketmeyi reddediyor ve SYSTEM task launch öncesinde installer file name + size + SHA-256 kimliğini doğruluyor. Task registration sonrasında, `Run($null)` çağrısının hemen önünde identity gate tekrar çalışıyor; artık launch sonrası hesaplanan hash trust gate olarak kullanılmıyor. `CanonicalDeployArtifactIdentityRegression.ps1` valid fixture'ı kabul edip byte mutation sonrasında aynı artifact'ı reddetti: `CANONICAL_DEPLOY_ARTIFACT_IDENTITY_GREEN`. `NativeInstallerSourceRegression.ps1` içinde `CanonicalDeployPinsBuiltArtifactIdentity=True`; build/deploy PowerShell AST parse GREEN.

2026-09-20 deep audit continuation evidence: #131 Git/archive/download canonical-routing gap'leri, #133 fast SYSTEM scheduled-task race, #134 manifest-ID collision ve #135 invalid-C# `validateSyntax=true` davranışı live temporary fixtures ile exact-installed runtime üzerinde yeniden üretildi. #132/#136/#137/#138/#139/#140 code-path/provenance audit bulgularıdır. Current Microsoft Task Scheduler docs `GetInstances`'ın yalnız currently-running instances döndürdüğünü ve `LastTaskResult`'ın son run sonucunu taşıdığını doğruluyor; npm current docs exact `package@version` metadata sorgusunu destekliyor. Kullanıcı açık kararı: routing/araç seçimi kısıtlanabilir; fakat genel Talvora capability ve explicit admin override yetenekleri kırpılmayacak.

141. [FIXED 2026-09-20 HTTP Download Uses Staged Durable Publish] `talvora_http_download` artık response body'yi destination ile aynı directory içindeki unique stage file'a yazar; existing destination non-resume path'te hiç truncate edilmez. Resume yolunda existing content stage'e kopyalanıp response segment'i stage'e append edilir. Body transferi tamamlandıktan sonra declared `Content-Length` varsa gerçek transferred byte count ile eşleşmesi doğrulanır, stage `Flush(true)` ile diske gönderilir ve SHA-256 stage üzerinde hesaplanır. Existing destination yalnız final `File.Replace`, absent destination yalnız final `File.Move` ile publish edilir; cancellation/network/body-length/disk hatasında stage cleanup olur ve old destination byte-for-byte kalır. Development source destination policy #131 ailesiyle mutation/network öncesi uygulanmaya devam eder.

2026-09-20 #141 remediation evidence: regression local loopback HTTP server `Content-Length: 1000` gönderip yalnız 10-byte body ile bağlantıyı kapattı. Download failure döndü; önceden var olan `Partial.cs` içeriği değişmedi ve `.talvora-download-*.tmp` stage artığı kalmadı. Full Source Edit targeted regression **48/48 GREEN**.

142. [FIXED 2026-09-20 Archive Extraction Uses Full Preflight, Isolated Staging, and Rollback-Capable Publication] `talvora_archive_extract` bütün entry path/type/conflict/policy/resource kontrollerini live destination mutation başlamadan önce tamamlıyor. File contents fresh temp staging root'a çıkarılıyor ve declared `ZipArchiveEntry.Length` ile actual staged length eşleştiriliyor. Publication öncesi her file için destination parent altında durable commit temp hazırlanıyor; existing target `File.Replace(commit,target,backup)`, new target handle-bound move ile yayınlanıyor. Commit zincirinin ilerleyen adımı hata verirse daha önce uygulanmış file steps reverse sırayla backup restore/delete ile geri alınıyor; transaction-created empty directories cleanup ediliyor.

2026-09-20 deep audit third-pass live evidence: #141 malformed/truncated local HTTP response ve #142 crafted two-entry ZIP exact-installed runtime üzerinde yeniden üretildi. Her iki probe da tool-level error sonrası destination'ın değişmiş kaldığını kanıtladı; bunlar yalnız routing problemi değil bağımsız failure-atomicity/data-integrity bug'larıdır.

143. [CLOSED FALSE POSITIVE 2026-09-20 RecoveryRequired Workspace Quarantine Is Implemented] Derin re-check'te current `SourceEditEngine` bütün canonical mutation girişlerinde (`ApplyPatchAsync`, `ApplyEditsAsync`, `ApplyGeneratedEditsAsync`) workspace lock alındıktan sonra ve yeni transaction lock/normalize/prepare öncesinde `RecoverWorkspaceLockedAsync` çağırıyor. Bu method store'daki aynı canonical workspace root'a ait state'leri enumerate ediyor; `LastEvent == "RecoveryRequired"` gördüğünde zero-mutation `TRANSACTION_RECOVERY_REQUIRED` üretiyor. Dolayısıyla farklı transactionId kullanmak quarantine sınırının dışında kalmıyor. Önceki #143 yorumu yalnız `ExecuteLockedAsync` içini değerlendirip üst seviye workspace preflight'i gözden kaçırdığı için yanlıştı. Not: corrupt journal enumeration davranışı ayrı #144 bulgusudur.

144. [FIXED 2026-09-20 Corrupt WAL Isolation / Workspace-Scoped Quarantine] `SourceEditTransactionStore` artık her transaction journal'ını bağımsız değerlendiriyor; geçersiz journal diğer state enumeration'ını durdurmak yerine durable `identity.json` + `quarantine.json` descriptor ile izole ediliyor. Workspace kimliği bilinen quarantine yalnız ilgili workspace canonical mutation'ını `TRANSACTION_RECOVERY_REQUIRED` ile fail-closed tutuyor; bağımsız workspace'ler ve diğer pending recovery işlemleri devam ediyor. Startup recovery `LastRecoveryQuarantines` üzerinden her quarantine'i structured log'a çıkarıyor. Dedicated regression bir workspace journal'ını complete-invalid record ile bozdu, ikinci bağımsız workspace editinin başarıyla commit edildiğini, ilgili workspace'in zero-mutation reddedildiğini ve quarantine descriptor'ın durable kaldığını doğruladı. Source Edit targeted regression 52/52 GREEN; Talvora Release 0 warning / 0 error.

145. [FIXED 2026-09-20 Managed MCP Recovery Redundancy Health / Self-Heal] Healthy-primary fast-path artık recovery manifests'i deterministic olarak yeniden seed ediyor; ownership `NeedsSeed` ise yalnız dosya varlığına bakmıyor, manifesti parse ediyor, registration validation çalıştırıyor ve expected registration ile canonical JSON içerik eşitliğini doğruluyor. Corrupt/stale/missing ownership copy otomatik reseed ediliyor. Dedicated ownership-health fixture fresh manifest -> healthy, deliberate corrupt content -> reseed required, PersistAsync sonrası -> healthy akışını GREEN doğruladı. Tray Release build 0 warning / 0 error.

2026-09-20 deep audit recovery note: #143 ve #144 current SourceEditEngine/TransactionStore control-flow inspection ile doğrulandı; regression suite taramasında `RecoveryRequired` workspace quarantine veya corrupt-WAL isolation assertion'ı bulunmadı. #145 registry/ownership store fast-path inspection bulgusudur. Bu bulgular için production mutation yapılmadı.

146. [FIXED 2026-09-20 talvora_move Destination-Side Source Ingress Is Policy-Routed] File move policy artık source ve destination sınıflandırmasını birlikte değerlendiriyor. Non-workspace source -> development-workspace source destination defaultta mutation öncesi `SOURCE_EDIT_POLICY_VIOLATION` ile duruyor; existing destination overwrite path'ine ulaşmıyor. `explicitAdmin=true` aynı ingress/move yeteneğini deliberate administration için koruyor.

2026-09-20 #125/#126/#131/#146 remediation evidence: targeted fixture nested `App/Use.cs -> Lib/UseMoved.cs`, source copy, outside -> workspace ingress, Git restore, archive extraction ve HTTP download-to-source default yollarının mutation öncesi canonical policy'ye döndüğünü doğruladı. Aynı fixture `explicitAdmin=true` copy/move/archive işlemlerinin başarılı kaldığını doğruladı. Routing Contract/guide normal source editing Avoid listesine `talvora_copy`, generalized `talvora_move`, `talvora_git_run` working-tree commands, `talvora_archive_extract`, `talvora_http_download` eklendi; ilgili tool descriptions doğrudan `talvora_apply_patch` yönünü taşıyor. Full Source Edit targeted regression **47/47 GREEN**.

2026-09-20 #146 live evidence: exact-installed runtime üzerinde temp non-workspace -> recognized development workspace existing `.cs` destination move `changed=true, overwrite=true` döndü; read_source sonrası target revision/content doğrudan değişmişti. Bu, #125 nested-workspace move ve #131 generic surface bulgularından bağımsız destination-side policy eksikliğini kanıtlar.

147. [FIXED 2026-09-20 Source Edit Parent Identity Is Pinned Through Commit and Recovery] Canonical Source Edit artık preflight/barrier sonrasında parent directory identity'yi transaction/recovery yaşam süresi boyunca durable volume/file-ID anchors ile bağlıyor. Existing ve transaction-created parent handle'ları `DELETE | FILE_READ_ATTRIBUTES` access, `FILE_FLAG_OPEN_REPARSE_POINT | FILE_FLAG_BACKUP_SEMANTICS` ve **FILE_SHARE_DELETE olmadan** açık tutuluyor; böylece parent rename/delete başka bir process tarafından guard kapanmadan yapılamıyor. Add/delete/simple-move handle-relative rename/disposition kullanıyor; update/edited-move ise `File.Replace` atomik metadata/ACL-preserving davranışını korurken parent path artık başka directory object'e çevrilemiyor. Crash recovery aynı `AcquireJournalPathGuard` invariantını kullanıyor; transaction sırasında yaratılan boş parent dizinleri handle'lar kapandıktan sonra güvenli biçimde temizleniyor.

2026-09-20 #147 remediation evidence: current Microsoft `CreateFile` contract `FILE_SHARE_DELETE` verilmezse delete/rename access isteyen sonraki open'ların sharing violation ile engellendiğini ve share mode'un handle kapanana kadar geçerli olduğunu doğruluyor; `ReplaceFile` contract atomik replacement ve original metadata/ACL preservation'ı doğruluyor. Dedicated fault-injection regressions delete, simple move, newly-created add parent, update/`File.Replace` ve edited-move/`File.Replace` sırasında gerçek `Directory.Move` denedi; beşinde de rename bloklandı ve intended transaction doğru hedefte tamamlandı. Full Source Edit targeted regression **42/42 GREEN**. Önceki live containment fixture'larının gerekli parent identity değişimi artık gerçekleşemediği için #147 kapatıldı.

148. [FIXED 2026-09-20 Canonical Build Uses Immutable Runtime Source Snapshot] Canonical build artık başlangıçta current HEAD object id, `git write-tree` index-tree identity ve bütün seçilmiş tracked/untracked runtime build input dosyalarının path+SHA-256 fingerprint'ini yakalıyor. Bu dosyalar `artifacts\installer-work\source-snapshot` altında ayrı build snapshot'ına kopyalanıyor; copy sonrası snapshot fingerprint'i ile canlı HEAD/index/full-input fingerprint'i başlangıç kimliğiyle yeniden karşılaştırılıyor ve capture sırasında source değişirse build fail-closed duruyor. Service, Tray ve Installer publish yolları ile dependency provenance okuması canlı repo yerine bu snapshot'a bağlandı; böylece build başladıktan sonra live working tree/HEAD transient değişse bile compiler mixed-source artifact üretemiyor. Payload ayrıca `source-snapshot.json` içinde headCommit/indexTree/runtimeInputsSha256/file-count/dirty fingerprint provenance taşıyor. Context7 + current Git docs HEAD, index ve `write-tree` semantiğini doğruladı. PowerShell AST parse GREEN; kalıcı `BuildScriptUsesImmutableSourceSnapshot=True` source contract ve tüm `NativeInstallerSourceRegression.ps1` GREEN. Gerçek canonical build snapshot path'lerinden service/tray/installer publish ederek exit 0 tamamlandı ve 261,736,207-byte `Talvora-Setup.exe` SHA-256 `ABC7AB270EF31D36AAEFB442ECFA12933EF58F4940C57FD9E8BF94E1434A40BE` üretti.

2026-09-20 deep audit fourth-pass evidence: #147 exact-installed runtime üzerinde iki aşamalı 32 MiB transaction + önceden hazırlanmış junction rename watcher ile delete/simple-move ve ayrı fault-injected missing-directory junction fixture ile add operasyonunda canlı yeniden üretildi. Delete varyantında transaction committed görünürken workspace dışı `Second.cs` silindi; simple move varyantında dış `Second.cs` workspace `Moved.cs` konumuna taşındı; add varyantında `workspace\NewDir` commit step öncesi junction yapıldı ve yeni `New.cs` dış dizinde oluşturuldu. Gerçek workspace originals korunurken transaction'lar success/committed raporladı. Bu canonical Source Edit containment modelinde gerçek CRITICAL TOCTOU bug'ıdır. Aynı yarışın update varyantı stage-file colocation nedeniyle safe rollback yaptı; edited-move ayrıca regression gerektirir. #148 build script control-flow + current Git semantics ve ayrıca temp clean-commit A/B fixture ile doğrulandı: HEAD değiştiği halde başlangıç/son fingerprint `null` kaldı.

149. [FIXED 2026-09-20 Background Job Logs and Completed Artifacts Are Bounded] `JobTools.PumpReaderAsync` artık stdout/stderr için 32 MiB segment sınırı kullanıyor; current segment dolduğunda önceki segment `.1` olarak tek-generation history biçiminde korunuyor ve yeni current segment açılıyor. Rotation generation sidecar'ı increment edilerek incremental reader'ın stale offset/generation durumunu algılaması sağlanıyor. Completed job storage 14 gün retention, 500 kayıt ve 4 GiB toplam kota ile temizleniyor. Cleanup live jobs'a dokunmuyor; servis restartından kalmış stale `Running` metadata gerçek process state'iyle `RefreshStateAsync` üzerinden reconcile ediliyor, exit zamanı bilinmeyen stale kayıt retention sıralamasında original `StartedAtUtc` değerini koruyor. Directory-size hesaplaması I/O/overflow durumunda fail-soft davranıp kaydı kota açısından en büyük kabul ediyor.

150. [FIXED 2026-09-20 job_read_output Uses a Bounded Continuation Response] `talvora_job_read_output` tek response için 4 MiB absolute ceiling kullanıyor; `maxBytes=0` artık “all” yerine bu bounded maximum chunk'ı seçiyor. Response `nextOffset`, `generation`, `resetRequired` ve `responseLimited` alanlarıyla pagination/rotation durumunu açıkça taşıyor. Reader generation değişiminde veya offset yeni segment uzunluğunu geçtiğinde byte 0'dan güvenli reset veriyor; rotation sırasında current file'ın kısa süreli yeniden açılma penceresi bounded retry ile ele alınıyor.

2026-09-20 #149/#150 remediation evidence: repo-dışı reflection fixture 30,000 karakterlik output'u 10,000-byte test segment budget'ıyla çalıştırdı; current=5424, previous=8192, generation=3 ve her iki segment budget altında kaldı. Aynı fixture `ReadOutputFromPathAsync` için 1000-byte test response ceiling'inde `responseLimited=true`, bounded `nextOffset` ve stale-generation `resetRequired=true` doğruladı. Retention fixture old-exited + stale-running kayıtlarını temizleyip yalnız `new-exited` kaydını tuttu: `JOB_BOUNDS_GREEN current=5424 previous=8192 generation=3 responseChars=1000 remaining=new-exited`. Son Talvora Release build **0 warning / 0 error**.

151. [FIXED 2026-09-20 Remote Tunnel Create Uses Durable Pending Provision/Reconciliation] Yeni tunnel oluşturma artık collision-proof managed-MCP identity altında crash-durable pending journal ile transaction sınırına alındı. Journal remote create başlamadan önce benzersiz 128-bit request marker + scope + registration kimliğini, create denemesi başlamadan hemen önce attempt state'ini, create/list sonucu tunnel ID öğrenildiğinde ise ID'yi caller cancellation'dan bağımsız olarak kaydediyor; bu ID activation delay ve local config/registry commit'ten önce durable hale geliyor. Retry bilinen pending ID'yi runtime key ile `admin --json tunnels get` üzerinden doğruluyor; ID bilinmeyen önceki create sonucu ise admin-key ile scope-filtered `tunnels list` ve beklenen tunnel adı + benzersiz request marker üzerinden reconcile ediliyor. Marker bulunamaz veya birden fazla eşleşme varsa otomatik ikinci create fail-closed engelleniyor; kör duplicate create yok. Registry commit tamamlanınca journal temizleniyor; commit sonrası crash journal bırakırsa sonraki registry pass aynı tunnel ID eşleşmesini görüp güvenle temizliyor. Pending ID varsa admin key sonradan kaldırılmış olsa bile runtime-key `get` ile recovery devam edebiliyor. Kalıcı `TunnelProvisioningPendingRegression.ps1` ordering/cancellation/reconciliation contract'ı GREEN; Talvora.Tray Release **0 warning / 0 error**. Gerçek hesaba audit amaçlı destructive create/delete yapılmadı.

152. [FIXED 2026-09-20 Collision-Proof Tunnel Config/Credential Fallback Identity] `ManagedMcpTunnelProvisioningService.ResolveConfigPath` explicit ConfigPath yokken artık ortak `ManagedMcpIdentityKey` SHA-256 key ile `%LocalAppData%\Talvora\ManagedTunnels\<key>\business.json` üretir; runtime credential ve state fallback alanları böylece farklı logical MCP ID'leri için ayrı köklere bağlanır. Repo-dışı reflection probe `foo/bar` ve `foo?bar` için iki farklı fallback directory doğruladı. Existing provisioned registrations explicit ConfigPath taşımaya devam eder; yeni fallback üretimi collision-resistant canonical key kullanır. Tray Release build 0 warning / 0 error.

2026-09-20 deep audit tunnel note: #151 remote account üzerinde destructive test yapılmadan current source control-flow + OpenAI tunnel-client current official CRUD contract karşılaştırmasıyla doğrulandı; gerçek hesaba audit amacıyla tunnel create/delete yapılmadı. #152 operational path derivation + connect identity flow code audit bulgusudur. DPAPI credential write ve business.json write kendi dosya seviyelerinde AtomicFile kullanıyor; sorun create ID'nin ilk durable kaydından önceki transaction boşluğu ve lossy persistent identity mapping'dir.

153. [FIXED 2026-09-20 Tunnel-Client Update Has Durable Cutover/Rollback Recovery] Release discovery/download `StageLatestTunnelClientAsync` olarak stage-only hale getirildi; normal `ConnectExistingAsync` ve periodic `EnsureLatestClientAndReconnectIfNeededAsync` aynı transactional cutover yoluna bağlandı ve candidate config readiness öncesi artık hiçbir yolda publish edilmiyor. `ExecuteClientUpdateTransactionAsync` stop-old öncesinde durable `.client-update.pending.json` journal'a exact previous/candidate `BusinessConfig` snapshot'larını yazıyor; ardından stop old -> publish candidate config -> candidate-configured journal -> exact candidate connect + `WaitForReadyAsync` -> committed journal sırası uygulanıyor. Candidate execution/ready/cancellation failure'ında previous config atomik restore ediliyor, alias state old client üzerinden idempotent stop ediliyor ve exact previous client/runtime caller cancellation'dan bağımsız olarak yeniden başlatılıyor. Compensation başarısızsa journal silinmeyip sonraki connect/update girişine bırakılıyor. Crash recovery current config candidate ise candidate readiness'i tekrar doğrulayıp commit ediyor; aksi halde previous config/runtime'a geri dönüyor. Başarı eventi yalnız terminal ready + committed journal sonrasında yazılıyor. Kalıcı `TunnelClientUpdateRollbackRegression.ps1` stage-only/no-precommit, transaction ordering, non-cancellable compensation, exact-old reconnect ve journal retention sözleşmelerini `TUNNEL_CLIENT_UPDATE_ROLLBACK_GREEN` doğruladı; Talvora.Tray Release **0 warning / 0 error**. OpenAI current tunnel-client docs da `runtimes connect` sonrasında `runtimes status`/health doğrulanmadan başarı raporlanmamasını öneriyor.

2026-09-20 tunnel update audit note: #153 current `EnsureLatestTunnelClientAsync -> DisconnectExistingAsync -> ConnectExistingAsync` control-flow inspection bulgusudur. Download binary publication kendi versioned path'inde yapılır ve official checksum doğrulanır; açık olan konu supply-chain hash değil, config/runtime cutover rollback semantiğidir.

154. [FIXED 2026-09-20 Managed MCP Primary Registry Refresh After Recovery] `ManagedMcpRegistryCoordinator` primary registry'nin gerçekten okunup okunmadığını ayrı `primaryAvailable` state'iyle izliyor. Backup/recovery kaynağından gelen registration seti discovered canonical set ile aynı olsa bile equality fast-path artık primary dosyayı `ManagedMcpRegistryStore.WriteAsync(... createBackup:false)` ile yeniden publish ediyor; healthy primary durumunda yalnız recovery copies reseed ediliyor. Dedicated temp-path contract missing primary ve invalid-schema primary senaryolarında canonical document'in tekrar okunabilir hale geldiğini doğruladı. Ownership health contract ile birlikte Tray Release build **0 warning / 0 error**, targeted redundancy probe GREEN.

155. [OPEN MEDIUM 2026-09-20 Gitea Running State Does Not Prove MCP Protocol Readiness] `GiteaTrayClient.GetStatusAsync` Gitea backend `3001/api/healthz`, Caddy `3000/api/healthz`, Gitea MCP `8081/healthz` ve tunnel health/ready HTTP endpoint'lerini kontrol edip hepsi 2xx ise `Running / Gitea MCP hazır` döndürüyor; gerçek MCP `initialize`, protocol version veya tools capability handshake'i yok. Live audit'te mevcut sistem ayrıca bağımsız olarak `POST 127.0.0.1:8081/mcp initialize` çağrısında HTTP 200, protocolVersion `2025-11-25`, serverInfo `Gitea MCP Server 1.7.0` ve tools capability döndürdü; yani **şu an** protocol sağlıklı, fakat status state-machine bunu kanıtlamıyor. Bir process health endpoint'i 200 verirken MCP route/session/protocol katmanı bozuk kalırsa Tray recovery false-green kalabilir. Fix yönü: bounded MCP initialize + tools/list/probe sonucunu kısa TTL cache ile Gitea readiness'e dahil et; ordinary dashboard refresh'te gereksiz process/browser side effect yaratma.

156. [OPEN HIGH 2026-09-20 Gitea Stop Can Report Whole Chain Offline After Only Backend Gitea Stops] `GiteaTrayClient.StopAsync` privileged `Stop-Chain` script'inden sonra `GetStatusAsync` poll ediyor; `GetStatusAsync` ilk olarak Gitea backend 3001'i probe ediyor ve bu probe başarısız olur olmaz Caddy, Gitea MCP task/process ve tunnel durumlarını hiç kontrol etmeden `Offline` döndürüyor. `StopAsync` bu tek koşulu “Gitea MCP zinciri ... elle durduruldu” terminal success olarak kabul ediyor. Dolayısıyla backend service kapanmış fakat scheduled task child/process/tunnel hâlâ yaşıyorsa stop operation false-success üretebilir ve manual-stop UI zincirin tamamen kapandığını söyleyebilir. Current live read-only evidence: audit anında `gitea`, `caddy`, `Gitea MCP Server`, `Gitea MCP Tunnel` ayrı ayrı Running ve 3000/3001/8081 listener'ları ayrı process'lerdi; yani bunlar tek backend-state ile özdeş değildir. Fix yönü: stop terminal gate bütün managed components için explicit stopped/not-listening identity check yapmalı; task stop sonrası owned child process termination/wait de bounded doğrulanmalı.

2026-09-20 deep audit managed-MCP/Gitea note: #154 coordinator control-flow bulgusudur; isolated path injection hook olmadığı için gerçek current-user registry deliberately bozulmadı. #155 current Gitea code-path gap + bağımsız live MCP initialize evidence ile doğrulandı; mevcut Gitea protocol şu an GREEN'dir. #156 read-only live task/service/listener identity + StopAsync early-return semantics karşılaştırmasıdır; audit amacıyla çalışan Gitea zinciri durdurulmadı. OpenAI current tunnel-client docs ayrıca `/healthz` liveness ile `/readyz` readiness'i ayırıyor ve health okumalarının MCP initialize yapmadığını açıkça belirtiyor; Talvora managed health tasarımında protocol-level kanıtı ayrı ele almak bu nedenle doğru modeldir.

157. [FIXED 2026-09-20 Shared Process Output Uses Bounded Head/Tail Capture] `ProcessRunner` artık shared `BoundedTextCapture` kullanıyor: default 4 Mi-character per-stream inline budget, absolute 16 Mi-character ceiling, retained head+tail ve explicit `StandardOutputTruncated/StandardErrorTruncated` metadata. Limit dolduktan sonra pipe tüketimi devam ediyor; yalnız response belleği büyümüyor. `InteractiveUserProcessRunner` iç helper da `ReadToEndAsync` ile RAM toplamak yerine loaded `Talvora.Shared.dll` içindeki `ProcessOutputPump` ile decoded text'i UTF-8 temp dosyalara async stream ediyor; LocalSystem parent bunları aynı bounded reader ile okuyor. Oversized-output regression truncation metadata + bounded retained content'i doğruladı; real interactive-user echo smoke GREEN. Shared, Talvora service ve Tray Release build **0 warning / 0 error**.

158. [FIXED 2026-09-20 Canonical talvora_read_source Uses Streaming Whole-File Metadata With Bounded Response Windows] `SourceEditEngine.ReadSourceAsync` artık ordinary source read için full `SourceFileSnapshot` materialize etmiyor. `SourceTextStreamingReader` aynı açık file handle üzerinde whole-file SHA-256'yı 128 KiB streaming buffer ile hesaplıyor; encoding sample'ından strict decoder seçiyor; newline/final-newline metadata'yı incremental `StreamReader.ReadAsync(Memory<char>, CancellationToken)` taramasıyla üretirken yalnız requested response window'u `StringBuilder` içinde tutuyor. Default response budget 1 Mi UTF-16 character, absolute ceiling 4 Mi character; `lineCount=0` EOF yönünde ilerlemeye devam ediyor fakat response ceiling'i aşınca `responseLimited=true`, `endReached=false`, `nextStartLine/nextStartCharacter` ile exact UTF-16 continuation döndürüyor. CRLF ve surrogate-pair tokenları response boundary'de parçalanmıyor. Existing mutation paths full text gerektiğinde `ReadSnapshotAsync` kullanmaya devam ediyor fakat `ReadAllBytesBoundedAsync` ile 256 MiB hard materialization ceiling'i allocation öncesi uyguluyor; revision-only snapshot yolu streaming hash kullanıyor.

159. [OPEN MEDIUM 2026-09-20 Multiple Diagnostic/Read Tools Expose 0=Unlimited Single-Response Modes Without an Absolute Transport/Memory Cap] #150 yalnız `job_read_output` için aynı sınıfı kaydetmişti; audit'te bunun daha geniş olduğu görüldü. `talvora_read_bytes count=0` kalan dosya uzunluğuna kadar tek `byte[]` allocate edip base64'e çeviriyor; `talvora_tail_text lineCount=0` bütün satırları listede tutup tek string join ediyor; generic HTTP response reader `maxBytes=0`, `talvora_tcp_exchange maxResponseBytes=0` ve `talvora_websocket_exchange maxMessageBytes=0` response body/message'ı `MemoryStream` içinde limitsiz büyütüyor; `talvora_sqlite_query maxRows=0`, `find_files/search_text max*=0` de tek structured MCP response'u limitsiz büyütebiliyor. Bu seçenekler TAM YETKI açısından yararlı olabilir ama tek invocation'ın server/serialization belleğini limitsiz tüketmesi gerekmiyor. Fix yönü: capability'yi kaldırmadan absolute per-response budget + pagination/continuation/artifact streaming; `0=unlimited` semantiği backend işi için korunacaksa transport response chunk'lanmalı ve truncation/continuation açık metadata olmalı.

2026-09-20 #158 remediation evidence: Microsoft current .NET docs `Stream.ReadAsync(Memory<byte>, CancellationToken)` ve incremental Decoder/Encoding APIs'in bounded buffer + cancellation modelini doğruluyor. Yeni regression 32 Ki-character tek satırda 4096-character response ceiling'i, exact `nextStartCharacter=4096`, whole-file revision equality ve ikinci continuation page'i doğruladı; ayrı 8192-byte fixture 4096-byte materialization budget'ını aşınca `SOURCE_EDIT_RESOURCE_LIMIT` aldı. Existing UTF-16 LE BOM + mixed newline regression yeni streaming read üzerinden GREEN kaldı. Full Source Edit targeted regression **53/53 GREEN**, regression project/Talvora Release build **0 warning / 0 error**. #159 diğer generic response surfaces için ayrı açık kayıttır.

160. [FIXED 2026-09-20 Process Timeout Covers Output Drain After Parent Exit] stdout/stderr pumps artık `WaitForExitAsync` ile aynı linked timeout/cancellation token'ını kullanıyor. Parent deadline'dan önce çıkmış olsa bile inherited output handle açık kaldığında bounded reader deadline'da kesiliyor; result `TimedOut=true` ve `OutputDrainTimedOut=true` ile dönüyor. Targeted regression parent'in `parent-exit` çıktısından sonra descendant'ın handle'ı açık tuttuğu senaryoda requested 2-second deadline'ın 6-second test ceiling'ini aşmadığını doğruladı; önceki yaklaşık 8.3-second late return artık oluşmuyor. Existing timeout/cancellation/quoting regressions ile birlikte `PROCESS_RUNNER_REGRESSION_GREEN`.

2026-09-20 #160 live evidence: repo dışı `TalvoraInheritedPipeAudit` helper current Talvora shared runner üzerinden çalıştırıldı. Requested timeout=2s; observed wall time yaklaşık 8.3s; result `timedOut=false`, parent exitCode=0. Helper child kendiliğinden sonlandı ve kalıcı process bırakılmadı.

161. [FIXED 2026-09-20 Contained Archive Commit Pins Reparse-Free Directory Identity] `allowOutsideDestination=false` path normalization/lexical containment kontrolüne ek olarak extraction fresh application-controlled staging root'ta yapılıyor. Live publication öncesi #147'nin volume/file-ID directory guard'ı target parent chain'i `FILE_FLAG_OPEN_REPARSE_POINT` ile açıp reparse directory components'i reddediyor, directory identity'lerini commit boyunca pinliyor ve parent rename/delete sharing değişimini engelliyor. New directories aynı guard altında oluşturulup tutuluyor. Existing reparse-point file target da contained mode'da replace öncesi reddediliyor. `allowOutsideDestination=true` deliberate unrestricted path capability olarak ayrı kalıyor.

162. [FIXED 2026-09-20 Archive Entry and Decompressed-Byte Budgets] Archive extraction artık `maxEntries`, `maxEntryBytes`, `maxTotalBytes` caller budgets taşıyor; `0` TAM YETKI davranışını korumak için Talvora'nın yüksek emergency ceiling'ini seçiyor. Server ceilings: 1,000,000 entries, 1 TiB per entry, 4 TiB aggregate declared decompressed bytes. Entry count ve `ZipArchiveEntry.Length` toplamı preflight'ta checked arithmetic ile doğrulanıyor; staged extracted file length ayrıca declared length ile eşleşmek zorunda. Budget failure destination tree oluşturulmadan sonlanıyor.

2026-09-20 #142/#161/#162 remediation evidence: Microsoft current ZIP/TAR best-practices dokümanı streaming API'lerde caller'ın entry-count/per-entry/aggregate decompressed-size limitleri koyması gerektiğini, lexical containment'ın destination veya parent reparse directory olduğunda tek başına yeterli olmadığını ve controlled destination kullanılmasını doğruluyor. Regression (1) ikinci destination file delete-sharing lock nedeniyle commit fail olduğunda ilk overwrite'ın backup'tan restore edildiğini, (2) contained destination altındaki directory symbolic-link component'in outside target'a hiçbir file yazılmadan reddedildiğini, (3) `maxEntries=1` ve `maxTotalBytes=9` budget failures'ın destination oluşmadan döndüğünü doğruladı. Full Source Edit targeted regression **51/51 GREEN**.

163. [FIXED 2026-09-20 FileSystemWatcher Completeness Loss Is Explicit and Acknowledgeable] `TalvoraWatchRuntime` artık managed queue `DroppedEvents` sayacından ayrı `OverflowCount` ve `ResyncRequired` state'i taşıyor. `FileSystemWatcher.Error` her durumda stream completeness'i şüpheli kabul edip `ResyncRequired=1` yapıyor; `InternalBufferOverflowException` ayrıca `OverflowCount` increment ediyor. Get/list/read response'ları bu state'i görünür kılıyor. Caller filesystem rescan tamamladıktan sonra `talvora_watch_read acknowledgeResync=true` ile flag'i açık biçimde temizleyebiliyor; response önceki resync state'ini ve acknowledgement sonucunu birlikte taşıyor. Bu model Microsoft'un watcher Error/internal-buffer overflow sonrasında event loss olabileceği sözleşmesiyle uyumlu.

164. [FIXED 2026-09-20 Watcher and HTTP Mock Persistent Resources Have Absolute Server Ceilings] Watcher `maxQueuedEvents=0` artık 100,000-event emergency ceiling seçiyor ve queue her durumda bounded kalıyor. HTTP mock caller limits'in 0 değerini high emergency ceilings'e çözüyor: per-listener capture queue 100,000 request, captured body 64 MiB, active handlers 512, manual pending requests 10,000 ve pending lifetime 24 saat; normal defaults 1000 / 2 MiB / 64 / 1000 / 30 saniye. `ListenLoopAsync` request başına limitsiz task üretmiyor: per-listener `HandlerSlots` ve process-wide 512-slot `GlobalHandlerSlots` birlikte backpressure uyguluyor ve slotlar handler `finally` bloğunda release ediliyor. Capture queue ayrıca process genelinde 100,000 request ve yaklaşık 512 MiB retained-memory emergency ceiling ile korunuyor; enqueue/dequeue/stop tek muhasebe yolundan global count/retained-byte state'ini güncelliyor. Manual pending kapasitesi ayrı `PendingSlots` ile bounded; body capture effective byte ceiling'e kadar tutulup kalan input drain ediliyor. Runtime info/read response'ları effective ceilings ile dropped/rejected sayaçlarını görünür taşıyor.

165. [FIXED 2026-09-20 AtomicFile Uses Durable Staged Publication] `AtomicFile` primary ve backup payload'larını same-directory staged temp üzerinde `FileOptions.WriteThrough` ile yazıyor ve `FileStream.Flush(flushToDisk:true)` ile intermediate buffers üzerinden diske geçiriyor. Publish aşaması managed `File.Move` + final-path reopen yerine Win32 `MoveFileExW` üzerinde `MOVEFILE_REPLACE_EXISTING | MOVEFILE_WRITE_THROUGH` kullanıyor; böylece yeni dosya görünür olduktan sonra başka bir reader'ın ikinci write-handle flush'ı sharing violation ile bozduğu belirsiz-success penceresi kaldırıldı. Backup publication da aynı staged/durable yol üzerinden ilerliyor ve temp cleanup korunuyor. Kalıcı `tests/AtomicFileDurabilityRegression.ps1` overwrite, old-content backup, pre-cancel zero-mutation ve temp-cleanup akışını `ATOMIC_FILE_DURABLE_REGRESSION_GREEN` doğruladı; Talvora.Shared Release **0 warning / 0 error**. Current Microsoft `Flush(true)` ve `MoveFileEx/MOVEFILE_WRITE_THROUGH` sözleşmeleri staged data flush ve write-through move davranışını doğruluyor.

2026-09-20 #163/#164 remediation evidence: Microsoft current FileSystemWatcher docs `Error` event'inin watcher devam edemediğinde veya internal buffer overflow olduğunda oluşabildiğini ve overflow halinde değişikliklerin kaybolabileceğini doğruluyor; current .NET `SemaphoreSlim` bounded concurrency primitive olarak kullanıldı. Repository targeted `--runtime-bounds-only` regression watcher queue cap=2 için bounded queue + dropped counter ve deterministic buffer-limit state için `OverflowCount=1/ResyncRequired=true` doğruladı. HTTP mock regression local queue trim, process-wide queue/retained-memory accounting ve finite local/global handler slots davranışını doğruladı; cleanup sonrası global counters baseline'a döndü. Talvora Release build **0 warning / 0 error**. `NativeInstallerSourceRegression.ps1` içindeki Watch/HTTP-mock bounded contract checks ve canonical Git routing contract birlikte **NATIVE_INSTALLER_SOURCE_GREEN** geçti.

166. [CLOSED DUPLICATE 2026-09-20 Evidence merged into #147] Bu madde #147 ile aynı Source Edit reparse/junction TOCTOU kök nedenini yeniden numaralandırmıştı. Friend-assembly fault-injection fixture tek-dosya ve multi-file late-step parent identity değişimini ayrıca doğruladı; yeni ayrı bug değildir. Canonical finding ve remediation kapsamı #147'dir; rollback/crash-recovery path'leri de aynı handle-bound identity invariantına dahil edilmelidir.

167. [CLOSED DUPLICATE 2026-09-20 Evidence merged into #148] Bu madde #148 ile aynı canonical installer source-snapshot/provenance yarışını yeniden numaralandırmıştı. Repo-dışı clean commit A -> clean commit B fixture current-HEAD fingerprint'in null -> null kalabildiğini ve captured SourceCommit'in stale kaldığını deterministik olarak doğruladı; yeni ayrı bug değildir. Canonical finding ve remediation kapsamı #148'dir.

2026-09-20 deep audit sixth-pass note: önce #166/#167 olarak açılan iki kayıt sırasıyla mevcut #147/#148'in duplicate'ı olduğu için kapatıldı ve deterministic fixture evidence canonical maddelere bağlandı. #147 için friend-assembly fault injector barrier-sonrası parent identity değişimini tek-dosya ve multi-file late-step akışında yeniden doğruladı. #148 için repo-dışı iki-clean-commit Git fixture HEAD A->B değişirken current-HEAD working-tree fingerprint'in null->null kalabildiğini ve captured provenance'in A'da kaldığını doğruladı. Production repo tree reset/revert/checkout edilmedi ve ürün kodu değiştirilmedi.

168. [FIXED 2026-09-20 Crash Recovery Durable File-Step Ownership] `SourceEditEngine` normal in-process failure rollback'unda `attemptedIndices`/`appliedIndices` ile yalnız gerçekten uygulanmış file step'lerini geri alıyor. Ancak durable `SourceEditJournalState` yalnız `LastEvent/LastSequence` taşıyor; `ReadStateAsync` önceki `CommitStepStarting`/`CommitStepApplied` olaylarının file-index geçmişini recovery modeline taşımıyor. `RecoverStateLockedAsync`, transaction bütünü after-state değilse planın **tüm** file kayıtlarını ters sırada `RollbackJournalFileAsync` ile işliyor. Bu özellikle henüz hiç uygulanmamış `add` step'inde ownership kaybına yol açıyor: `RollbackAddAsync`, final path varsa ve content revision planlanan `AfterRevision` ile eşleşiyorsa dosyayı siliyor; o step'in Talvora tarafından uygulanmış olduğuna dair durable kanıt aramıyor. Repo-dışı friend-assembly fixture'da üç adımlı transaction'ın yalnız file-0 update'i commit edildikten sonra simulated crash üretildi; file-1 add hiç çalışmadı, file-2 update before-state'te kaldı. Crash sonrasında dış writer file-1 hedefini planlanan add ile byte-for-byte aynı içerikle oluşturdu. Recovery file-2 nedeniyle rollback yoluna girdi ve hiç uygulanmamış file-1 add kaydını da rollback ederek dış writer dosyasını sildi; file-0/file-2 doğru before-state'e döndü. Fix yönü: WAL recovery file-step ownership bilgisini durable olarak yeniden kurabilmeli. `CommitStepStarting`/`CommitStepApplied` history file index bazında preserve edilmeli; hiç attempted olmayan step live target'a dokunmadan yalnız stage/temporary artifact cleanup yapmalı. Crash commit operation ile `CommitStepApplied` kaydı arasında gerçekleşebileceği için yalnız applied event de yeterli değildir; started-but-not-recorded-complete step için backup/stage/final revision + operation-specific artifact identity ile 'Talvora applied / not applied / indeterminate' kararı verilmeli, indeterminate durumda external data değiştirilmeden `RecoveryRequired` üretilmelidir. Add/update/delete/move için crash-before-step, crash-during-step ve crash-after-operation-before-event regressions eklenmeli.

2026-09-20 #168 live evidence: `C:\Windows\Temp\TalvoraRecoveryOwnershipAudit` fixture Release çalıştırmasında `AFTER_CRASH_ADD_EXISTS=False`, dış writer sonrasında `EXTERNAL_CREATED_EXISTS=True`, recovery sonrasında `AFTER_RECOVERY_EXTERNAL_EXISTS=False` gözlendi; first/third dosyaları `first0`/`third0` before-state'e döndü. Fixture repo source'unu değiştirmedi.

169. [OPEN MEDIUM 2026-09-20 Dependency Provenance Can Drift From the Already-Published Binary After a Later Restore] `Build-Windows-Installer.ps1` service ve Tray publish'lerini tamamladıktan sonra `Get-ResolvedPackageManifest` ile her projenin **mevcut** `obj\project.assets.json` dosyasını okuyup `resolved-dependencies.json` üretir. Microsoft'un current .NET/NuGet sözleşmesinde `project.assets.json` restore tarafından üretilen geçici dependency graph'ıdır ve `dotnet publish` implicit restore çalıştırabilir. Bu assets dosyası published binary ile immutable olarak bağlanmış değildir; publish sonrasında ayrı bir IDE/CLI restore aynı `obj` dosyasını yeniden yazabilir. Current build gate `Assert-RuntimeBuildInputsUnchanged` Git runtime inputs'i denetler; `obj/project.assets.json` build-tree artifact'ını source fingerprint'e bağlamaz. Repo-dışı local-NuGet fixture'da consumer `Audit.Dep Version="*"` ile feed'de yalnız 1.0.0 varken publish edildi; published `Consumer.deps.json` `Audit.Dep/1.0.0` taşıdı. Ardından aynı feed'e 2.0.0 eklendi ve publish output değiştirilmeden `dotnet restore --force-evaluate` çalıştırıldı. `obj/project.assets.json` 2.0.0'a geçti ve canonical `Get-ResolvedPackageManifest` ile aynı mantık `version=2.0.0` raporlarken published deps hâlâ 1.0.0 kaldı. Böylece `resolved-dependencies.json` binary'nin gerçek runtime dependency setini yanlış temsil edebilir. Fix yönü: tek build içindeki resolved graph publish ile aynı immutable restore snapshot'ına bağlanmalı. Explicit restore sonucunu build-local immutable artifacts path'e alıp hashle; sonraki publish'leri `--no-restore` ile o snapshot üzerinden yürüt ve provenance üretirken aynı assets snapshot/hash'i kullan. Runtime package/version listesi ayrıca published `.deps.json` ile cross-check edilmeli; intentional floating versions build'ler arasında korunabilir ama **aynı build** içinde resolved graph değişmemelidir. Concurrent IDE/restore regression'ı eklenmeli.

2026-09-20 #169 live evidence: `C:\Windows\Temp\TalvoraDependencyProvenanceAudit` local-feed fixture `PUBLISHED_BEFORE=Audit.Dep/1.0.0`, bağımsız restore sonrası `ASSETS_AFTER_RESTORE=Audit.Dep/2.0.0`, `MANIFEST_FUNCTION_WOULD_REPORT={name:Audit.Dep,version:2.0.0,direct:true}` ve published deps'in hâlâ 1.0.0 olduğunu doğruladı. Fixture repo source'unu değiştirmedi.

170. [FIXED 2026-09-20 Complete Invalid WAL Tail Fails Closed] `SourceEditTransactionStore.ReadStateAsync` son journal satırında JSON/base64/checksum decode hatası oluştuğunda exception türünü ayırmadan `break` yapıyor. Bu davranış yarım kalmış son append'i tolere etmek için tasarlanmış olsa da **tam ve parse-edilebilir bir WAL envelope'unda yalnız checksum'ın geçersiz olması** da aynı şekilde önceki event'e düşürülüyor. Özellikle son durable event `RecoveryRequired` ise state geçici olarak önceki `Prepared`/`RollingBack` event'i gibi görülebiliyor. `RecoverStateLockedAsync` bu downgraded state üzerinden yeni `RollingBack` event'ini append edebiliyor ve ilk rollback file-step'ini uygulayabiliyor; checksum-invalid eski terminal satır artık journal'ın ortasında kaldığında ancak bir sonraki append/read sırasında hard failure görünür hale geliyor. Repo-dışı friend-assembly fixture'da iki-file update planına durable `RecoveryRequired` yazıldı; final envelope yalnız checksum alanı değiştirilerek tam satır halinde bırakıldı. `ReadStateAsync` `RecoveryRequired -> Prepared` döndü. File-0 before-state, file-1 after-state + valid before-backup olacak şekilde recovery tetiklendi. Yeni transaction sonuçta tekrar `TRANSACTION_RECOVERY_REQUIRED` ile reddedildi, fakat bundan **önce** reverse rollback file-1'i `second-after` içeriğinden `second-before` içeriğine gerçekten çevirdi ve backup'ı tüketti. Bu, integrity-invalid terminal kaydında fail-closed/no-mutation sözleşmesinin ihlalidir. Fix yönü: yalnız gerçekten incomplete/torn tail güvenilir biçimde ayırt edilebiliyorsa son record ignore edilmeli. Parse edilebilir envelope + checksum mismatch, valid base64 ama payload-integrity mismatch veya tam satır structural inconsistency her zaman immediate `RecoveryRequired` olmalı ve recovery hiçbir live mutation başlatmamalı. Daha sağlam formatta framed record length + checksum + durable record terminator/commit marker kullanılabilir. Terminal `RecoveryRequired` state ayrıca ayrı durable summary/receipt ile cross-check edilebilir. Regressionlar: truncated partial tail tolerated-without-mutation, complete checksum-invalid tail fail-closed, checksum-invalid terminal state zero-mutation, mid-journal invalid record isolation.

2026-09-20 #170 live evidence: `C:\Windows\Temp\TalvoraWalTailAudit` ilk fixture `STATE_BEFORE_CORRUPTION=RecoveryRequired`, `STATE_AFTER_BAD_FINAL_CHECKSUM=Prepared` gösterdi; yeni transaction sonunda quarantine yeniden oluştu. İkinci `C:\Windows\Temp\TalvoraWalTailMutationAudit` fixture aynı downgrade sonrasında `SECOND_BEFORE_ENGINE=second-after`, final sonuç `recovery-required`, fakat `SECOND_AFTER_ENGINE=second-before` ve `BACKUP_EXISTS=False` göstererek quarantine reassert edilmeden önce rollback mutation gerçekleştiğini doğruladı. Repo source değiştirilmedi.

171. [FIXED 2026-09-20 Completed-Transaction Tombstone Preserves Idempotency Identity] Canonical Source Edit sözleşmesi aynı `transactionId + requestHash` retry için prior committed receipt döndürüp `replayed=true` vermeyi ve değişikliği yeniden uygulamamayı vaat ediyor. Ancak `SourceEditTransactionStore.CleanupCompletedAsync` committed receipt/journal'ları 30 gün retention sonrasında ve ayrıca `MaximumCompletedTransactions=10_000` / `MaximumCompletedStateBytes=512 MiB` kotası aşıldığında tamamen siliyor. Receipt ve transaction directory birlikte kalkınca transactionId için kalıcı tombstone/requestHash identity kalmıyor. Repo-dışı friend-assembly fixture'da bir update transaction ilk çağrıda committed/replayed=false, immediate retry'da replayed=true döndü. Receipt timestamp'i 31 gün geriye alınıp `CleanupCompletedAsync` çalıştırıldığında hem receipt hem journal directory silindi. Dosya bağımsız olarak original before-state'e getirildikten sonra **aynı transactionId ve aynı request body** tekrar gönderildi; işlem yeniden committed oldu, `replayed=false` döndü ve edit ikinci kez uygulandı. Bu yalnız 30 günlük yaşa bağlı değildir; count/size eviction aynı identity kaybını daha erken oluşturabilir. Fix yönü: ağır receipt/WAL retention ayrı tutulabilir fakat idempotency identity için küçük, durable tombstone/index (`transactionId digest + requestHash + terminal status + minimal receipt identity`) çok daha uzun veya açıkça sözleşmeye bağlı retention ile korunmalı. Eğer idempotency bilinçli olarak bounded olacaksa public/architecture contract kesin süre/kota semantiğini açıkça belirtmeli ve expiry sonrası aynı transactionId'i yeni işlem gibi sessizce kabul etmek yerine stable `TRANSACTION_ID_EXPIRED`/unknown-history durumu döndürmeyi değerlendirmeli. Count/size eviction ve time-expiry sonrası same-request regression eklenmeli.

2026-09-20 #171 live evidence: `C:\Windows\Temp\TalvoraIdempotencyRetentionAudit` fixture `FIRST_STATUS=committed`, `FIRST_REPLAYED=False`, immediate retry `IMMEDIATE_REPLAYED=True`; aged cleanup sonrası `RECEIPT_AFTER_CLEANUP=False`, `JOURNAL_DIR_AFTER_CLEANUP=False`; file before-state'e döndürüldükten sonra aynı transaction retry `RETRY_STATUS=committed`, `RETRY_REPLAYED=False` ve content tekrar `after` oldu. Repo source değiştirilmedi.
2026-09-20 remediation evidence: Source Edit regression **40/40 GREEN** ve Release build **0 warning / 0 error**. #168 için WAL step ownership history + operation-specific applied/not-applied/indeterminate recovery kararı eklendi ve unattempted external same-content add korunuyor. #170 için yalnız gerçekten newline-terminator almamış fiziksel final fragment incomplete-tail olarak tolerans görüyor; tam checksum-invalid record immediate fail-closed. #171 için heavy receipt/journal cleanup öncesi minimal durable transaction tombstone yazılıyor ve retired transactionId tekrarında `TRANSACTION_ID_EXPIRED` ile zero-mutation reddediliyor. #147 için directory volume/file-ID anchors WAL'a taşındı ve pre-operation identity drift deterministic fixture'larda zero-mutation conflict'e dönüştü; fakat residual path-based primitive penceresi nedeniyle #147 halen OPEN.
